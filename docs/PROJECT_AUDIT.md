# Auditoria Permanente do Projeto

> Este arquivo é o registro permanente de auditoria, planejamento, decisões e progresso técnico da Narrative Platform. Todo console futuro do Claude Code deve lê-lo integralmente, junto com todos os arquivos `CLAUDE.md` aplicáveis, antes de alterar o projeto.

---

## 1. Protocolo obrigatório para consoles futuros

Todo console futuro que trabalhar neste projeto deve, antes de qualquer alteração:

1. Ler todos os `CLAUDE.md` aplicáveis (raiz, `backend/CLAUDE.md`, `frontend/CLAUDE.md`).
2. Ler este arquivo (`docs/PROJECT_AUDIT.md`) integralmente.
3. Verificar `git status` com comandos somente de leitura.
4. Preservar qualquer alteração manual/local ainda não commitada — nunca descartar.
5. Executar apenas o escopo do console atual; não expandir sem necessidade real.
6. Atualizar este arquivo ao final do console (nova entrada datada, nunca sobrescrever histórico).
7. Responder e planejar em português; código-fonte, comentários e documentação técnica normal em inglês.
8. Não apagar histórico deste arquivo — apenas adicionar/atualizar estados com data.
9. Não afirmar sucesso (build, teste, deploy) sem evidência real de execução.
10. Distinguir sempre: **confirmado** (visto no código), **inferido** (deduzido, não testado), **planejado** (ainda não implementado) e **desconhecido** (não foi possível verificar).

### Regras para atualização futura deste arquivo

1. Nunca apagar entradas históricas.
2. Nunca substituir uma decisão antiga sem registrar que ela foi revisada.
3. Registrar novas conclusões como entradas datadas.
4. Atualizar estados quando uma feature for implementada.
5. Preservar a diferença entre confirmado / inferido / planejado / desconhecido.
6. Registrar todas as migrations novas.
7. Registrar endpoints adicionados ou alterados.
8. Registrar decisões de segurança.
9. Registrar testes adicionados e testes realmente executados, separadamente.
10. Registrar problemas encontrados durante cada console.
11. Registrar alterações manuais identificadas no início do console.
12. Não incluir secrets, senhas, tokens ou valores sensíveis.
13. Não transformar o arquivo em uma cópia integral do código.
14. Manter o arquivo útil, objetivo e incremental.

---

## 2. Metadados da auditoria

- **Data/hora**: 2026-07-30.
- **Branch**: `main`.
- **Commit no início do console**: `90882d4` ("feat(chronicle): fit dragon rows dynamically and frame the panel").
- **Working tree no início**: limpo (`nothing to commit, working tree clean`).
- **Executado por**: Claude Code (console de auditoria).
- **Escopo**: auditoria geral somente leitura do repositório inteiro (raiz, `backend/`, `frontend/`, infraestrutura, migrations, testes) **mais** um conjunto de limpezas pontuais explicitamente autorizadas pelo utilizador durante este mesmo console (ver §12, console nº1).
- **Restrições aplicadas**: sem Maven/Gradle/Java CLI, sem iniciar Spring Boot, sem executar migrations, sem alterar banco de dados, sem comandos Docker que criem/parem/modifiquem containers, sem instalar dependências, sem alterar `.env`, sem `git push`, sem descartar alterações locais. Um único commit local foi explicitamente autorizado pelo utilizador para as ações do §12.

Nenhum secret foi reproduzido neste arquivo. `backend/src/main/resources/application-local.yml` (não versionado, listado em `backend/src/main/resources/.gitignore`) contém uma chave OpenAI e um segredo JWT com aparência real — não reproduzidos aqui; recomenda-se ao utilizador confirmar que essas credenciais não são reaproveitadas em produção e, por precaução, rotacioná-las.

---

## 3. Resumo executivo

**Estado geral**: o projeto é um MVP funcional de uma plataforma narrativa colaborativa para parties de RPG, com um único módulo em produção real de features ("Arquivo do Cronista"): contas, parties, convite reutilizável por party, crônicas escritas (com bloqueio de edição) e crônicas-jogo por turnos com moderação do narrador e geração final por IA. A infraestrutura documentada (Render) está desatualizada; a real é Vercel (frontend) + Cloudflare Tunnel (backend), corrigida neste console.

**Maturidade**: sólida no núcleo de jogo (parties, membros, convites, turnos, ciclos, segmentos ocultos/removidos, IA), mas com lacunas de qualidade transversais: praticamente zero cobertura de testes de backend (1 arquivo de teste em todo o projeto) e de frontend (2 arquivos, ambos sobre tema), nenhuma proteção de rate limiting, nenhum cabeçalho de segurança (CSP/HSTS), e nenhuma infraestrutura de eventos/outbox genérica (a fila de IA é o único padrão de job existente).

**Principais áreas completas**: autenticação username/senha, parties e papéis (`OWNER`/`NARRATOR`/`PLAYER`), convite reutilizável por party, crônicas escritas com lock de 24h, crônicas-jogo com turnos/ciclos/expiração/skip/finalização, visibilidade de segmentos (ocultos, revelação do narrador, remoção/moderação), pipeline de IA pós-conclusão com 3 artefatos independentes (mapa do cânone, adaptação, sinopse — Console nº6, requer validação manual/execução de testes), tema visual "Arcane Editorial Nocturne" (claro/escuro/sistema).

**Principais áreas pendentes**: votação (histórias e tags) — totalmente ausente, greenfield (agora com uma base real de tags para votar, ver Console nº6); PWA e Web Push — totalmente ausente, greenfield; notificações internas — totalmente ausente, greenfield; badges/ranking — ausente.

**Principais riscos**: (a) `/dev-preview` era uma rota de desenvolvimento sem guarda de ambiente, reestruturada neste console (ver §12) para nunca mais ser commitada; (b) cobertura de testes quase nula em toda a base; (c) ausência de rate limiting em endpoints sensíveis (login, registro, regeneração de convite); (d) tabelas órfãs no schema (`audit_events`, `party_invites`) sem código Java correspondente; (e) documentação de infraestrutura estava desatualizada (Render), corrigida neste console.

**Recomendação principal de sequência**: antes de iniciar qualquer feature nova de votação, notificação ou PWA/push, tratar a dívida técnica transversal (testes de backend nas áreas de turno/IA, rate limiting básico, `Clock` bean para testabilidade) reduziria risco, mas não é bloqueante. Tecnicamente, a sequência mais segura é: (1) revisão integrada das funcionalidades existentes, (2) página da história finalizada + extração de tags (dependem de dados de IA já existentes), (3) votos em histórias e tags (podem ser paralelos entre si, mesmo padrão arquitetural), (4) auditoria + base de PWA (pré-requisito técnico de Web Push), (5) Web Push e notificações (dependem do PWA), (6) revisão final. Ver §6 para o detalhamento item a item da lista de 19 itens fornecida pelo utilizador.

---

## 4. Arquitetura atual confirmada

### Frontend
- Next.js **16.2.11**, React **19.2.0**, App Router (`frontend/app/`), sem Pages Router.
- Rotas: `/` (`frontend/app/page.tsx`, login+registro combinados), `/invite/[token]` (`frontend/app/invite/[token]/page.tsx`), `/app` (`frontend/app/app/page.tsx`, dashboard de parties), `/app/party/[partyId]` (`frontend/app/app/party/[partyId]/page.tsx`, arquivo da party), `/app/party/[partyId]/chronicle/[chronicleId]` (`frontend/app/app/party/[partyId]/chronicle/[chronicleId]/page.tsx`, branch `GameView`/`WrittenView` por `?type=`).
- Layout único: `frontend/app/layout.tsx` (fontes, script inline anti-flash de tema, `ThemeProvider`). Não há layouts aninhados por rota.
- "Shell" autenticado é um componente cliente, não um layout: `frontend/components/AppShell.tsx` — busca `GET /auth/me` a cada montagem e redireciona para `/` em falha; não há `middleware.ts` nem guarda de rota centralizada.
- Cliente HTTP: `frontend/lib/api.ts` (`api<T>()`), injeta `Authorization: Bearer` a partir de `frontend/lib/auth.ts` (token em `localStorage`, chave `narrative-platform-token`), sempre `cache: "no-store"`, converte `ProblemDetail` em `ApiError` tipado.
- Sem `loading.tsx`/`error.tsx` em nenhuma rota; estados de loading/erro/vazio são markup manual por página.
- Tema **Arcane Editorial Nocturne**: confirmado em `frontend/app/globals.css` (tokens `:root` + bloco `:root[data-theme="dark"]`), mecanismo em `frontend/lib/theme.ts`, `frontend/lib/theme-init-script.ts`, `frontend/components/ThemeProvider.tsx`, `frontend/components/ThemeToggle.tsx`. **Concluído — não deve ser reimplementado.**
- PWA/service worker/manifest/Push: **ausentes** (sem `frontend/public/`, sem `manifest`, sem `sw.js`, zero uso de `Notification`/`PushManager` no código).
- Votação (histórias/tags): **ausente** (zero ocorrências de "vote"/"voto"/"ranking"/"badge").
- IA no frontend: renderização inline em `frontend/app/app/party/[partyId]/chronicle/[chronicleId]/page.tsx` (bloco `.generated-story`, botão de regeneração, estados `AI_PENDING`/`AI_PROCESSING`); tipo `GeneratedStory` em `frontend/lib/types.ts`.
- Convite no frontend: painel real em `frontend/app/app/party/[partyId]/page.tsx` (link reutilizável, regeneração), com botão de WhatsApp (`frontend/components/WhatsAppIcon.tsx`, deep link `wa.me`) mantido deliberadamente por decisão do utilizador (§12).
- Testes: Vitest (`frontend/vitest.config.ts`), apenas 2 arquivos — `frontend/components/ThemeToggle.test.tsx`, `frontend/lib/theme.test.ts`. Sem testes de `lib/api.ts`, `lib/auth.ts`, `AppShell` ou páginas.

### Backend
- Spring Boot **4.1.0**, Java **21**, Maven (`backend/pom.xml`). Pacote raiz `com.narrativeplatform`.
- 5 bounded contexts em `backend/src/main/java/com/narrativeplatform/app/`: `auth`, `party`, `invitation`, `chronicle`, `aijob`. Não existem contexts de `voting`, `notification` ou `webpush`.
- Controllers: `AuthController` (`/api/auth`), `PartyController` (`/api/parties`), `InvitationController` (`/api/parties/{partyId}/invitation`, `/api/invites/{token}`), `ChronicleController` (`/api/parties/{partyId}/chronicles`, inclui `/regenerate` de IA).
- Serviço autoritativo de turno/ciclo/expiração/finalização: `GameChronicleService` (`backend/src/main/java/com/narrativeplatform/app/chronicle/services/GameChronicleService.java`) — `GameRunEntity.currentSequence` + o único `GameTurnEntity` `ACTIVE` é a fonte de verdade; `advance()`/`completeRun()` finalizam a run e enfileiram IA; `expireTurns()` (`@Scheduled(fixedDelay=60_000)`) varre expirações; `requireCurrentTurn` também expira turnos preguiçosamente antes da varredura.
- Autorização: `PartyAccessService` (`requireActiveMember/requireNarrator/requireOwner`) e `ChronicleAccessService`, sem `@PreAuthorize`/interceptor — chamadas explícitas em cada serviço.
- Erros: `GlobalExceptionHandler` (`@RestControllerAdvice`) + hierarquia `DomainException` (`BadRequestException`, `ForbiddenException`, `NotFoundException`, `ConflictException`, `TurnExpiredException`, `AiNotConfiguredException`), tudo via `ProblemDetail`.
- IA: `AiJobService`/`AiJobStateService`/`AiJobProcessor` (`@Scheduled(fixedDelay=15_000)`)/`OpenAiClient` (`backend/src/main/java/com/narrativeplatform/shared/integrations/OpenAiClient.java`). Funciona sem `OPENAI_API_KEY` (jobs ficam `PENDING`; regeneração explícita responde `503 ai_not_configured`).
- Sem `Clock` bean (todo timestamp é `Instant.now()` direto). Sem rate limiting. Sem `ApplicationEventPublisher`/outbox genérico — a tabela `ai_jobs` é o único padrão de fila existente.
- Testes: **1 único arquivo** em todo o backend — `backend/src/test/java/com/narrativeplatform/app/invitation/services/InvitationServiceTest.java`.

### Base de dados
- PostgreSQL via Flyway, migrations em `backend/src/main/resources/db/migration/`: `V1__initial_schema.sql`, `V2__game_turn_sequence_deferrable.sql`, `V3__party_invitation_links.sql` (detalhamento em §8).
- Tabelas órfãs (sem entidade JPA): `audit_events` (parece outbox abandonado, colunas `event_type`/`payload_json` sem código Java) e `party_invites` (modelo antigo de convite de uso único, substituído por `party_invitation_links`).

### Infraestrutura
- **Frontend**: Vercel — **confirmado pelo utilizador nesta sessão** (não estava documentado em nenhum arquivo do repositório antes deste console).
- **Backend**: hospedado onde o processo Spring Boot roda, exposto publicamente via **Cloudflare Tunnel** (`cloudflared`) — **confirmado pelo utilizador nesta sessão**; não havia nenhuma menção a Cloudflare em todo o repositório antes deste console (confirmado por grep).
- **Render**: não é mais usado. `render.yaml` (raiz) foi removido e `SETUP-PTBR.md` foi corrigido neste console (§12).
- `backend/Dockerfile`: build multi-stage (`maven:3.9.11-eclipse-temurin-21` → `eclipse-temurin:21-jre`), expõe porta 8080. Continua válido como forma de empacotar o backend, independentemente de onde ele seja executado.
- Sem Docker Compose, sem CI/CD (`.github/workflows` inexistente) em nenhum ponto do repositório.

### Integrações
- OpenAI: `shared/integrations/OpenAiClient`, configurável por `OPENAI_API_KEY`/`OPENAI_MODEL`/`OPENAI_BASE_URL`, opcional.
- Resend (e-mail) e WhatsApp Business API: **não existem no backend** — confirmado por grep (`Resend|wa\.me|WhatsApp|InviteChannelType|PartyInviteEntity`, zero ocorrências). Um documento antigo (já removido, ver §12) recomendava essa remoção; aparentemente já aconteceu sem atualização da documentação correspondente.

### Testes (visão consolidada)
- Backend: 1 arquivo de teste (`InvitationServiceTest`), sem testes de controller, repository ou de turnos/IA/segmentos.
- Frontend: 2 arquivos de teste, ambos sobre tema (`ThemeToggle`, `theme.ts`).
- Nenhum teste end-to-end em nenhum lugar do repositório.

---

## 5. Funcionalidades confirmadas como implementadas

| Funcionalidade | Estado | Evidência | Arquivos principais | Observações |
|---|---|---|---|---|
| Registro/login username+senha | Concluída | `AuthController`, Argon2id via Spring Security | `backend/.../auth/controllers/AuthController.java`, `AuthService` | — |
| Parties + papéis (`OWNER`/`NARRATOR`/`PLAYER`) | Concluída | `PartyEntity`, `PartyMemberEntity`, `PartyAccessService` | `backend/.../party/**` | — |
| Convite reutilizável por party | Concluída | Migration `V3`, `PartyInvitationLinkEntity`, `InvitationService` | `backend/.../invitation/**`, `frontend/app/app/party/[partyId]/page.tsx` | Substituiu o modelo antigo de convite único; documentação antiga (já removida) ainda descrevia o modelo velho. |
| Crônicas escritas + lock de edição 24h | Concluída | `WrittenStoryDocumentEntity`, `WrittenChronicleService` | `backend/.../chronicle/**` | — |
| Crônicas-jogo (turnos, ciclos 1–3, ordem embaralhada) | Concluída | `GameRunEntity`, `GameTurnEntity`, `GameChronicleService.create` | `backend/.../chronicle/services/GameChronicleService.java` | Ordem: criador primeiro, depois embaralhado, repetido por ciclo. |
| Expiração/skip de turno | Concluída | `expireTurns()` scheduled + `requireCurrentTurn` lazy | idem | — |
| Entrada de membro em jogo em andamento | Concluída | `insertPartyMemberIntoActiveRuns` | idem | Motivou a migration `V2` (constraint deferrable). |
| Segmentos ocultos / revelação do narrador / moderação | Concluída | `GameSegmentEntity`, `SegmentRevisionEntity`, `toHiddenResponse/toPublicDisabledResponse` | idem | Revelação de 10s é regra de frontend; backend trata dado revelado como intencional. |
| Fila de jobs de IA (geração da história final) | Concluída | `AiJobEntity`, `AiJobStateService`, `AiJobProcessor`, `OpenAiClient` | `backend/.../aijob/**`, `shared/integrations/OpenAiClient.java` | Só existe para crônicas-jogo, não para crônicas escritas. |
| Tema Arcane Editorial Nocturne (claro/escuro/sistema) | Concluída | Ver §4 (Frontend) | `frontend/app/globals.css`, `frontend/lib/theme*.ts`, `frontend/components/Theme*.tsx` | Não reimplementar. |
| Pipeline de IA após a conclusão (mapa do cânone + adaptação + sinopse) | Concluído (Console nº6) — requer validação manual/execução de testes | 3 tarefas independentes (`ai_jobs.job_type`), `party_ai_tag_settings`, `canon_map_generations`/`canon_tags`/`canon_tag_sources`, `chronicle_synopses` | `backend/.../app/aijob/**`, `backend/.../app/canon/**`, `backend/.../app/chronicle/services/{ChronicleSynopsisService,AiArtifactsQueryService}.java`, `frontend/components/chronicle/{CanonMapPanel,AiTagSettingsPanel}.tsx` | Substitui o antigo item "Extração automática de tags". Absorve também a antiga "Mini-história manual" — ver nota abaixo. |
| Votos em histórias (backend/frontend) | Ausente | Grep sem ocorrências, sem tabela | — | Greenfield. |
| Lista, ranking e badges | Ausente | Grep sem ocorrências | — | Greenfield; depende de votos. |
| Votos em tags (backend/frontend) | Ausente | Grep sem ocorrências | — | Greenfield; depende de extração de tags. |
| PWA instalável / manifest / service worker | Ausente | Sem `frontend/public/`, sem manifest, sem SW | — | Greenfield. |
| Notificações internas (backend) | Ausente | Sem entidade/tabela/serviço | — | Greenfield. |
| Assinaturas Web Push + VAPID | Ausente | Sem entidade/config | — | Greenfield; depende de PWA. |
| Página da história finalizada (estado dedicado) | Requer validação manual | Mesmo endpoint de detalhe (agora com `creatorName`/`createdAt`/`completedAt`) serve todos os status; sem rota pública de compartilhamento; layout dedicado (cabeçalho de conclusão, thread completa, Mapa do cânone acima da thread + Adaptação da história em modal) reformulado no Console 3 (ver §12) | `frontend/app/app/party/[partyId]/chronicle/[chronicleId]/page.tsx`, `frontend/components/chronicle/*` | Build, lint e testes automatizados passaram no Console 2 e novamente no Console 3; validação visual real em navegador ainda não foi executada em nenhum dos dois consoles (sem acesso a browser tooling no Console 3). |
| Barra de progresso de turnos (crônicas-jogo) | Concluída | `GameProgressBar`, calculada a partir de turnos com status diferente de `WAITING`/`ACTIVE` | `frontend/lib/progress.ts`, `frontend/components/chronicle/GameProgressBar.tsx` | Aparece abaixo do título na página da crônica (em andamento e finalizada) e acima do título de cada card `GAME`/`IN_PROGRESS` no Arquivo do Cronista; backend expõe o total via `ChronicleCardResponse.completedTurns/totalTurns` (Console 3). |
| Histórico de versões da história gerada por IA | Concluída (backend + seletor no modal) | `generated_stories` já era versionada (nunca sobrescrita); endpoint novo expõe o histórico | `GET /api/parties/{partyId}/chronicles/{chronicleId}/generated-stories`, `frontend/components/chronicle/StoryModal.tsx` | Mostra sempre a versão mais recente por padrão; seletor só aparece com mais de uma versão. |
| Ordenação inteligente do Arquivo do Cronista | Concluída (Console 4) | `compareChronicles` no frontend + `ChronicleCardResponse.createdAt/awaitingCurrentUser` novos no backend | `frontend/app/app/party/[partyId]/page.tsx`, `backend/.../chronicle/services/ChronicleService.java`, `GameTurnRepository.findActiveTurnUsersByChronicleIds` | Pendentes antes de concluídos, sem separador visual; dentro dos pendentes, crônicas onde é a vez do usuário logado vêm primeiro, depois mais antigas primeiro; dentro dos concluídos, mais novas primeiro. |
| Logging de depuração no backend (`@Slf4j`) | Concluída (Console 4) | Turnos/ciclos (`GameChronicleService`) e fila de IA (`AiJobService`/`AiJobStateService`/`AiJobProcessor`/`AiJobWorker`) instrumentados | `backend/src/main/resources/application-local.yml` (`logging.level.com.narrativeplatform: DEBUG`) | Produção (`application.yml`) não ganhou nenhum override — continua silenciosa por padrão; só o perfil local do desenvolvedor liga o nível DEBUG. |
| E-mail (Resend) / WhatsApp Business (envio automático) | Ausente no backend / Legada no frontend | Grep sem ocorrências no backend; botão WhatsApp ainda ativo no frontend | `frontend/components/WhatsAppIcon.tsx` | Mantido deliberadamente no frontend por decisão do utilizador (§12) — não é lacuna, é escolha de produto. |
| `/dev-preview` (sandbox de FE sem backend) | Reestruturada neste console | Ver §12 | `.claude/commands/fe-preview.md`, `frontend/CLAUDE.md` | Deixou de ser rastreada pelo git; recriável sob demanda pelo comando `/fe-preview`. |

---

## 6. Roadmap pendente auditado

Lista original fornecida pelo utilizador, cruzada com o estado real encontrado:

1. **Auditoria geral do projeto** — Executada neste console (console nº1). Ver §12.
2. **Página da história finalizada** — Estado real: parcial (ver tabela §5). Sem dependências bloqueantes; pode ser refinada agora. Nenhuma migration esperada. Reutiliza `GameChronicleService.detail()`/`WrittenChronicleService` e os componentes `GameView`/`WrittenView` já existentes.
3. **Pipeline de IA após a conclusão** (antigo "Extração automática de tags") — **Concluído no Console nº6** (ver §12). `ai_jobs` generalizado com discriminador `job_type`; três handlers independentes (`AdaptationJobHandler`, `CanonMapJobHandler`, `SynopsisJobHandler`); migration `V4`.
4. **Mini-história manual** — **Removida do roadmap no Console nº6.** A adaptação literária automática (item 3, artefato 2) já cobre a necessidade original ("uma versão organizada/legível da thread"); o utilizador não pediu, neste console, uma via adicional de edição manual da adaptação. Se no futuro se quiser uma edição manual da adaptação (distinta da regeneração por IA), isso deveria voltar ao roadmap como um item novo e explícito, não como retomada deste.
5. **Votos em histórias (backend)** — Ausente, greenfield. Não depende de votos em tags. Reutilizar `PartyAccessService`/`ChronicleAccessService` para autorização. Migration nova esperada. Risco a evitar: referenciar `segment_id`/`turn_id` (estáveis), nunca `sequence_number` (renumerado em entradas/saídas de membros).
6. **Lista, ranking e badges** — Depende de votos em histórias (item 5) já existirem. Sem migration própria além da de votos, possivelmente uma projection/view para ranking.
7. **Votos em tags (backend)** — Depende de extração de tags (item 3) já existir. Independente de votos em histórias (item 5), mas mesmo padrão arquitetural (poderiam ser desenvolvidos em paralelo depois que tags existirem).
8. **Interface de votos nas tags** — Depende do item 7 (backend) estar pronto.
9. **Revisão integrada das funcionalidades** — Recomendado antes de iniciar PWA/notificações, para consolidar o núcleo de jogo/IA/votos antes de empilhar mais uma camada (push) sobre uma base ainda não validada ponta a ponta.
10. **Auditoria para PWA e Web Push** — Já parcialmente coberta por este documento (§4, §5); um console dedicado pode aprofundar detalhes de manifest/service worker específicos quando a implementação começar.
11. **PWA instalável** — Greenfield. Pré-requisito técnico de Web Push (item 13/16). Não depende de nenhuma outra feature da lista.
12. **Notificações internas (backend)** — Greenfield. Pode ser desenvolvida com o mesmo padrão de fila usado por `aijob` (`AiJobEntity`/`AiJobStateService`/`AiJobProcessor` como modelo estrutural). Migration nova esperada.
13. **Assinaturas Web Push e VAPID** — Depende de PWA instalável (item 11) para existir um service worker registrado. Migration nova esperada (tabela de subscriptions).
14. **Ativação de notificações — frontend** — Depende dos itens 12 e 13 (backend) existirem.
15. **Eventos do jogo e notificações** — Pontos de gancho já identificados no código: `GameChronicleService.advance()`/`completeRun()`, `InvitationService.acceptForUser`. Depende de notificações internas (item 12) existirem como infraestrutura.
16. **Worker de Web Push** — Depende de assinaturas Web Push (item 13) e notificações internas (item 12). Pode espelhar `AiJobProcessor` como padrão de scheduled dispatcher.
17. **Preferências e aviso de expiração** — Depende de notificações (item 12) e Web Push (item 13/16) existirem.
18. **Central de notificações** — Depende de notificações internas (item 12) existirem; é majoritariamente frontend consumindo endpoints já definidos no item 12.
19. **Revisão final de PWA, segurança e documentação** — Deve vir por último, depois que PWA/push/notificações estiverem implementados; é o momento de tratar cabeçalhos de segurança (CSP/HSTS) que hoje não existem em lugar nenhum do projeto.

**Sequência tecnicamente recomendada** (não segue a ordem numérica acima, pois há dependências reais): 1 (feito) → 9 → 2 → 3 → 4 → 5 e 7 (podem ser paralelos entre si) → 6 e 8 → 10 → 11 → 12 e 13 (podem ser paralelos) → 14, 15, 16, 17, 18 → 19.

---

## 7. Mapa de domínio

- **Parties**: `PartyEntity` (tabela `parties`), papéis `PartyRoleType{OWNER,NARRATOR,PLAYER}`, status `MemberStatusType{ACTIVE,DISABLED,REMOVED}` em `PartyMemberEntity` (tabela `party_members`). Serviço: `PartyService`, `PartyAccessService`.
- **Memberships**: mesma entidade acima. `DISABLED` é reversível; `REMOVED` é permanente para o vínculo (requer novo convite).
- **Invitations**: `PartyInvitationLinkEntity` (tabela `party_invitation_links`, PK = `party_id`, um link ativo por party). Serviço: `InvitationService`. Tabela antiga `party_invites` (V1) é órfã, sem entidade JPA.
- **Chronicles**: `ChronicleEntity` (tabela `chronicles`), tipos `GAME`/`WRITTEN`. Serviços: `ChronicleService` (genérico/listagem), `GameChronicleService`, `WrittenChronicleService`.
- **Turns**: `GameTurnEntity` (tabela `game_turns`), `GameRunEntity` (tabela `game_runs`), `GameDraftEntity` (tabela `game_drafts`). Fonte autoritativa: `GameChronicleService`.
- **Segments**: `GameSegmentEntity` (tabela `game_segments`), `SegmentRevisionEntity` (tabela `segment_revisions`, auditoria de edição/desabilitação).
- **AI**: `AiJobEntity` (tabela `ai_jobs`), `GeneratedStoryEntity` (tabela `generated_stories`). Serviços: `AiJobService`, `AiJobStateService`, `AiJobProcessor`, `OpenAiClient`.
- **Nota (Console nº2)**: nenhuma mudança de modelo de domínio — `GameChronicleDetailResponse` passou a expor `creatorName`/`createdAt`/`completedAt`, todos já existentes em `ChronicleEntity`/`GameRunEntity`, sem nova coluna, tabela ou invariante.
- **Votes**: nenhuma entidade/tabela/serviço existe — greenfield.
- **Notifications**: nenhuma entidade/tabela/serviço existe — greenfield. `audit_events` (V1) é uma tabela órfã que parece ter sido uma tentativa abandonada de outbox genérico, sem nenhum código Java associado.

---

## 8. Migrations existentes

| Arquivo | Propósito | Tabelas | Dependências | Observações |
|---|---|---|---|---|
| `V1__initial_schema.sql` | Schema inicial completo | `users`, `parties`, `party_members`, `party_invites`, `chronicles`, `written_story_documents`, `written_story_permissions`, `game_runs`, `game_turns`, `game_drafts`, `game_segments`, `segment_revisions`, `generated_stories`, `ai_jobs`, `audit_events` | Nenhuma (base) | Contém um bloco `DO $$ ... IF NOT EXISTS ... END $$` para adicionar a FK `chronicles.current_generated_story_id → generated_stories.id` de forma idempotente (FK circular entre `chronicles` e `generated_stories`) — precedente a seguir em migrations futuras que toquem `chronicles`. `party_invites` e `audit_events` são órfãs (sem entidade JPA). |
| `V2__game_turn_sequence_deferrable.sql` | Torna `uq_game_turn_sequence` (`game_turns`, colunas `run_id, sequence_number`) `DEFERRABLE INITIALLY DEFERRED` | `game_turns` (alter) | Depende de `V1` | Necessária para permitir renumeração em massa de `sequence_number` quando membros entram/saem de jogos em andamento (`insertPartyMemberIntoActiveRuns`/`removePartyMemberFromActiveRuns`). |
| `V3__party_invitation_links.sql` | Cria o modelo de convite reutilizável por party | `party_invitation_links` (nova) | Depende de `V1` (`parties`, `users`) | Coexiste com a tabela antiga `party_invites` (V1), que ficou órfã mas não foi removida. |

**Próximo número seguro para novas migrations**: `V8__*.sql`.

| `V4__ai_job_types_and_canon_pipeline.sql` (Console nº6) | Generaliza `ai_jobs` (+ `job_type`) e cria a infraestrutura do pipeline de IA pós-conclusão | Altera `ai_jobs` (+`job_type`), altera `chronicles` (+`synopsis`, +`current_synopsis_id`); cria `party_ai_tag_settings`, `chronicle_synopses`, `canon_map_generations`, `canon_map_generation_categories`, `canon_tags`, `canon_tag_sources` | Depende de `V1` (`chronicles`, `parties`, `game_segments`, `ai_jobs`) | Ver §12 (Console nº6) para detalhamento completo de colunas/constraints. `party_ai_tag_settings` foi removida na `V6` (ver abaixo) — este passo original está historicamente superado. |
| `V5__game_participants.sql` (Console nº7) | Cria o rastreio de participação por crônica-jogo (quem saiu/voltou de um run específico, e quem removeu) | Cria `game_participants` (`run_id`, `user_id`, `status`, `removed_by_type`, `removed_by_user_id`, `left_at`), com backfill de uma linha `ACTIVE` por usuário que já tem turno em cada run existente | Depende de `V1` (`game_runs`, `users`) | Referencia `run_id`/`user_id` (nunca `sequence_number`), respeitando a restrição de design já registrada em §9. |
| `V6__dynamic_canon_categories.sql` (Console nº8) | Substitui o enum fechado de 5 categorias por categorias dinâmicas por party (nome livre, descrição, cor em hex) | Cria `canon_categories` (nova, live config); backfill a partir de `party_ai_tag_settings` (`enabled=true`) e depois `DROP TABLE party_ai_tag_settings`; altera `canon_map_generation_categories` (troca `category`/`color`/`enabled` enum por `name`/`description`/`color` hex, mesmo mecanismo de snapshot imutável); altera `canon_tags` (troca `category`/`color` enum por FK `category_snapshot_id → canon_map_generation_categories.id`) | Depende de `V4` (todas as tabelas do pipeline de IA/cânone) | Ordem crítica: o backfill de `canon_tags.category_snapshot_id` (join pelo enum antigo) acontece **antes** de `canon_map_generation_categories` dropar suas colunas enum antigas. Ver §12 (Console nº8). |
| `V7__party_spectator_invitation_links.sql` (Console nº9) | Amplia `party_invitation_links` de "um link por party" para "um link por (party, papel-alvo)", suportando um convite de Espectador paralelo ao de Jogador | Adiciona `id` (nova PK substituindo `party_id`) e `target_role` a `party_invitation_links`; troca a PK antiga por `UNIQUE(party_id, target_role)` | Depende de `V3` (`party_invitation_links`) | Sem geração de token em SQL — o link de espectador de parties já existentes é criado sob demanda pela própria aplicação (mesmo mecanismo de "buscar ou criar" já usado hoje) na primeira vez que o dono abrir o painel de convites. Ver §12 (Console nº9). |

**Risco de compatibilidade a observar**: qualquer migration futura que remova/renomeie `party_invites` ou `audit_events` deve primeiro confirmar (com o utilizador) se essas tabelas têm dados em produção que precisam ser preservados ou migrados, já que não há evidência de quando ou se elas deixaram de ser escritas.

---

## 9. Decisões técnicas existentes

| Decisão | Fonte | Impacto | Estado |
|---|---|---|---|
| Cada party tem exatamente um link de convite reutilizável, regenerável por narrador ativo ou dono | `backend/CLAUDE.md` (Domain Invariants), confirmado em `PartyInvitationLinkEntity`/`InvitationService` | Substitui modelo de convite único; simplifica UX de convite | Confirmado, em produção |
| Token bruto do convite é persistido (não só o hash) para redisplay; `token_hash` é usado na resolução pública | `backend/CLAUDE.md`, Javadoc de `PartyInvitationLinkEntity` | Trade-off de segurança deliberado e documentado | Confirmado |
| Aplicação deve iniciar sem `OPENAI_API_KEY`; jobs automáticos ficam pendentes, regeneração explícita retorna 503 `ai_not_configured` | `backend/CLAUDE.md`, `AiJobService`/`OpenAiClient` | Backend nunca quebra por falta de chave de IA | Confirmado |
| `DISABLED` é reversível, `REMOVED` é permanente para o vínculo | `backend/CLAUDE.md`, `PartyMemberEntity`/`MemberStatusType` | Fonte de verdade para reativação vs. novo convite | Confirmado |
| `sequence_number` não é estável (é renumerado); features futuras devem referenciar `segment_id`/`turn_id` | Inferido do código (`V2` + `insertPartyMemberIntoActiveRuns`) | Restrição de design para votação/qualquer feature que referencie um turno/segmento específico | Confirmado no código, decisão a ser respeitada por features novas |
| WhatsApp/Resend removidos do backend | Confirmado por grep (zero ocorrências) | Nenhum código morto de integração externa no backend | Confirmado |
| Botão de WhatsApp permanece no frontend, sem alteração | Decisão do utilizador neste console (§12) | Reverte a recomendação de um documento antigo (já removido) que sugeria remover também do frontend | Decisão confirmada nesta sessão |
| Infraestrutura real é Vercel (frontend) + Cloudflare Tunnel (backend), não Render | Confirmado pelo utilizador neste console; corrigido em `SETUP-PTBR.md` e `render.yaml` removido | Documentação agora reflete a realidade de deploy | Confirmado nesta sessão |
| `/dev-preview` deixa de ser commitado; recriável sob demanda pelo comando `/fe-preview` | Decisão do utilizador neste console (§12) | Padroniza testes visuais de frontend sem backend, sem risco de ir para produção | Confirmado nesta sessão |
| `frontend/lib/api.ts` trata qualquer corpo de resposta 2xx vazio como sucesso sem corpo (lê texto primeiro, só faz `JSON.parse` se não vazio), em vez de assumir corpo só quando `status === 204` | Bug real encontrado pelo utilizador no Console 4 (§12): `POST .../regenerate` retorna `202` sem corpo, e o código antigo chamava `response.json()` incondicionalmente para qualquer status "ok" diferente de 204 | Corrige `"Regenerar história"`/`"Re-executar IA"`; cobre também qualquer futuro endpoint 2xx sem corpo | Confirmado nesta sessão |
| Logging de backend via `@Slf4j` fica silencioso por padrão em produção; só liga com `logging.level.com.narrativeplatform: DEBUG` no perfil local (`application-local.yml`) ou na variável `LOGGING_LEVEL_COM_NARRATIVEPLATFORM` | Decisão do utilizador neste console (Console 4, §12), mecanismo escolhido pelo assistente entre as duas opções sugeridas | Depuração local mais rica sem nenhum custo/risco em produção | Confirmado nesta sessão |
| Ordenação do Arquivo do Cronista é sempre pendentes-antes-de-concluídos, sem separador visual entre os grupos; dentro dos pendentes, `awaitingCurrentUser` (turno ativo pertence ao usuário logado) tem prioridade sobre a data de criação | Decisão do utilizador neste console (Console 4, §12) | Substitui a ordenação anterior (só `updatedAt DESC` do backend); cálculo de `awaitingCurrentUser` é `GAME`+`IN_PROGRESS`-only | Confirmado nesta sessão |

---

## 10. Riscos e dívidas técnicas

**Crítico**: nenhum identificado neste console.

**Alto**:
- Cobertura de testes do backend praticamente inexistente (1 arquivo de teste em todo o projeto) — área de turnos/ciclos/IA é a mais rica em invariantes de negócio e a menos testada. *(Testes)*
- Ausência de rate limiting em endpoints públicos sensíveis (login, registro, resolução/aceite de convite). *(Segurança)*

**Médio**:
- Ausência de cabeçalhos de segurança (CSP, HSTS, X-Frame-Options) em toda a aplicação. *(Segurança)*
- Tabelas órfãs `audit_events` e `party_invites` sem entidade JPA correspondente — dívida de schema, risco de confusão em migrations futuras. *(Dados/Arquitetura)*
- `application-local.yml` local (não commitado) contém segredos em texto simples no disco do desenvolvedor — recomenda-se rotação por precaução. *(Segurança)*
- Cobertura de testes do frontend limitada a tema; `lib/api.ts`, `lib/auth.ts`, `AppShell` e páginas sem nenhum teste. *(Frontend/Testes)*

**Baixo**:
- Ausência de `Clock` bean no backend (todo timestamp é `Instant.now()` direto), dificultando testes determinísticos de expiração/turnos. *(Arquitetura)*
- Sem `ApplicationEventPublisher`/outbox genérico — qualquer notificação futura precisará ser um novo padrão de fila (reaproveitando a forma de `aijob`) ou aumentar acoplamento entre services via chamada direta. *(Arquitetura)*
- Documentação de infraestrutura estava desatualizada (Render) até este console; corrigida agora, mas vale conferir se outros documentos futuros (fora deste repositório) ainda referenciam Render. *(Infraestrutura)*

---

## 11. Perguntas em aberto

- Onde exatamente o processo do backend roda hoje (VPS própria, servidor doméstico, outro provedor) por trás do Cloudflare Tunnel? O utilizador confirmou o uso de Cloudflare, mas não o host subjacente nem um arquivo de configuração `cloudflared` versionado no repositório.
- As tabelas órfãs `party_invites` e `audit_events` têm dados em produção que precisam ser preservados, migrados ou podem ser dropados com segurança em uma migration futura?
- Existe algum SLA/expectativa de disponibilidade para o backend que deveria influenciar decisões de infraestrutura (ex.: monitoramento, health checks automatizados além do `/actuator/health` já exposto)?
- A "página da história finalizada" (item 2 do roadmap) tem um design/spec definido além do que já existe hoje, ou o próximo console deve começar por levantar requisitos de produto antes de codificar? *(Respondida no Console 2 — o utilizador forneceu uma especificação completa; ver §12.)*
- `CanonMapPanel` deixou de ser um placeholder no Console nº6 — agora busca `GET .../ai-artifacts` e renderiza categorias/tags reais, com todos os estados (pending/processing/failed/IA não configurada/desabilitado/vazio/com dados). O botão "Re-executar IA" continua regenerando **só a adaptação** (não há ação de regenerar mapa/sinopse neste console — decisão registrada no Console nº6, §12).
- **(Nova, Console nº6)** Durante este console, dois arquivos (`AiJobType.java`, `AiJobWorker.java`) foram editados externamente (pelo utilizador ou por um hook/linter) enquanto a IA aguardava resposta a uma pergunta, introduzindo um modelo de IA diferente por tipo de tarefa (`AiJobType.getModelByType()`, literais `"gpt-5.6-sol"`/`"gpt-5.6-terra"`/`"gpt-5.6-luna"`, hardcoded no enum) e propagando um parâmetro `model` por `OpenAiClient.generate`/`AiJobWorker.processOne`/`AiJobProcessor`. Essas mudanças foram preservadas e completadas (corrigido `OpenAiClient.generate` para reportar o `model` realmente usado em vez do `properties.model()` global, que ficou órfão). **Pergunta genuína para o utilizador**: os três nomes de modelo (`gpt-5.6-sol`/`-terra`/`-luna`) são placeholders reais de algum provedor específico, ou nomes temporários/fictícios que precisam ser substituídos antes de qualquer chamada real à IA? Isso não foi testado com uma chamada de IA de verdade nesta sessão.
- **(Nova, Console nº6)** `mvn test` não pôde ser executado neste console: `settings.xml` do Maven local aponta para um mirror corporativo privado (`main.nexus.timwe.com`, sem resolução de DNS nesta rede), e falta em cache o artefato `org.apache.maven.surefire:surefire-junit-platform:jar:3.5.6`, necessário só para *executar* testes (compilar `mvn compile`/`mvn test-compile` funcionou normalmente, pois esses artefatos já estavam em cache). Isso é uma limitação de ambiente pré-existente, não relacionada ao código deste console — recomenda-se ao utilizador rodar a suíte de testes localmente pelo IntelliJ (que provavelmente usa um mirror/proxy diferente ou tem o artefato já resolvido) para confirmar que os testes novos realmente passam.
- `Segment`/`Turn` (respostas `GameSegmentResponse`/`GameTurnResponse`) não incluem `@username` do autor, só `displayName`. A especificação do Console 2 pedia a exibição de `@username` na thread; como o dado não existe na resposta atual, ele foi omitido (não inventado) — se o produto quiser mesmo mostrar `@username` na thread, isso exige um pequeno acréscimo de campo nesses dois response records em um console futuro.

---

## 12. Histórico dos consoles

### Console nº1 — Auditoria geral inicial + limpeza de infraestrutura/documentação

- **Data**: 2026-07-30.
- **Objetivo original**: auditoria geral somente leitura do repositório e inicialização deste arquivo.
- **Objetivo ampliado durante o console**: o utilizador, após revisar o plano, autorizou explicitamente ações adicionais de limpeza (fora do escopo somente-leitura original): remoção de `render.yaml`, atualização de `SETUP-PTBR.md` para refletir Vercel + Cloudflare Tunnel (em vez de Render), absorção do conteúdo relevante de 3 documentos legados neste arquivo seguida da exclusão deles, e reestruturação da rota `/dev-preview` do frontend.
- **Arquivos lidos**: `CLAUDE.md` (raiz), `backend/CLAUDE.md`, `README.md` (raiz, backend, frontend), `PROJECT-STATUS.md`, `CODEBASE-AUDIT-PRE-IMPLEMENTATION.md`, `PWA-PUSH-AUDIT.md`, `SETUP-PTBR.md`, `render.yaml`, `.gitignore` (raiz), `backend/pom.xml` (via exploração), `backend/src/main/resources/application.yml` (via exploração), migrations `V1`/`V2`/`V3`, `frontend/package.json` (via exploração), `frontend/app/dev-preview/page.tsx`, e leitura ampla de código-fonte de `backend/src/main/java/com/narrativeplatform/**` e `frontend/app`, `frontend/components`, `frontend/lib` via 3 agentes de exploração somente leitura.
- **Arquivos alterados/criados/apagados**:
  - Criado: `docs/PROJECT_AUDIT.md` (este arquivo).
  - Apagado: `render.yaml` (raiz).
  - Apagados (conteúdo relevante absorvido acima): `CODEBASE-AUDIT-PRE-IMPLEMENTATION.md`, `PWA-PUSH-AUDIT.md`, `PROJECT-STATUS.md`.
  - Modificado: `SETUP-PTBR.md` (Render → Cloudflare Tunnel).
  - Modificado: `.gitignore` (raiz) — adicionada entrada para `frontend/app/dev-preview/`.
  - Removido do tracking do git: `frontend/app/dev-preview/page.tsx`.
  - Criado: `frontend/CLAUDE.md`.
  - Criado: `.claude/commands/fe-preview.md`.
- **Conclusões**: ver §3 (resumo executivo), §5 (funcionalidades) e §10 (riscos). O núcleo de jogo/IA está maduro; votação, PWA/push e notificações são greenfield; a maior dívida transversal é a quase total ausência de testes de backend.
- **Testes executados**: nenhum (console de auditoria/documentação; execução de Maven/Java/testes não autorizada neste console).
- **Testes não executados**: toda a suíte de backend (Maven) e frontend (`npm run lint`/`npm run build`/`vitest`) — não executados neste console.
- **Próximo passo**: ver §13.

---

### Console nº2 — Página da história finalizada (história-jogo)

- **Data**: 2026-07-30.
- **Objetivo**: implementar a apresentação de leitura de uma história-jogo finalizada — cabeçalho com metadados de conclusão, thread original completa como conteúdo principal, e (abaixo dela) duas colunas com um painel placeholder "Mapa do cânone" e o painel "Adaptação da história" já existente. Não incluiu extração de tags, votos, ranking, PWA, notificações ou mini-história manual, e não tocou em `WrittenView`/crônicas escritas.
- **Achado inicial importante**: no início deste console, `GameChronicleDetailResponse.java` já continha uma edição local não commitada (de uma tentativa anterior nesta mesma sessão) adicionando os campos `creatorName`/`createdAt`/`completedAt` ao record, mas o call-site em `GameChronicleService.detail()` não havia sido atualizado — o backend não compilava no estado em que o working tree foi encontrado. Isso foi corrigido como primeiro passo, preservando a edição já existente.
- **Arquivos lidos**: `CLAUDE.md` (raiz), `backend/CLAUDE.md`, `frontend/CLAUDE.md`, este arquivo (`docs/PROJECT_AUDIT.md`) integralmente, além de leitura direta de: `GameChronicleService.java`, `GameChronicleDetailResponse.java`, `GameSegmentResponse.java`, `GameTurnResponse.java`, `GeneratedStoryResponse.java`, `GameSegmentEntity.java`, `ChronicleEntity.java`, `GameRunEntity.java`, `ChronicleStatusType.java`, `GameTurnStatusType.java`, `GameRunStatusType.java`, `ChronicleAccessService.java`, `ChronicleController.java`, `ChronicleService.java`, `ChronicleRepository.java`, `PartyAccessService.java`, `AiJobStatusType.java`, `AiJobStateService.java`, `AiJobProcessor.java`, `GlobalExceptionHandler.java`, `DomainException.java`, `NotFoundException.java`, `ForbiddenException.java`, `frontend/app/app/party/[partyId]/chronicle/[chronicleId]/page.tsx`, `frontend/lib/types.ts`, `frontend/lib/api.ts`, `frontend/components/AppShell.tsx`, `frontend/components/DragonPanel.tsx`, `frontend/app/app/party/[partyId]/page.tsx`, `frontend/app/globals.css`, `frontend/vitest.config.ts`, `frontend/vitest.setup.ts`, `frontend/components/ThemeToggle.test.tsx`, `frontend/package.json`.
- **Arquivos alterados**:
  - `backend/src/main/java/com/narrativeplatform/app/chronicle/services/GameChronicleService.java` — completado o call-site de `GameChronicleDetailResponse` com `creatorName`/`createdAt`/`completedAt`.
  - `frontend/lib/types.ts` — `GameDetail` ganhou `creatorName: string`, `createdAt: string`, `completedAt?: string`.
  - `frontend/app/app/party/[partyId]/chronicle/[chronicleId]/page.tsx` — `GameView` reescrito: busca paralela de `GameDetail` + `PartyDetail` (mesmo padrão já usado por `WrittenView`), estado de erro de carregamento via `ApiError` (403/404/genérico) substituindo o antigo travamento indefinido em "Consultando a crônica...", branch `finished = status !== "IN_PROGRESS"` que troca o cabeçalho para `ChronicleCompletedHeader` e passa a exibir o bloco de duas colunas (`CanonMapPanel` + `AiAdaptationPanel`) apenas quando finalizada, thread convertida para `<ol>/<li>` envolvendo `ThreadSegmentRow`. `WrittenView` não foi alterada.
  - `frontend/app/globals.css` — fix da linha conectora pendurada no último item da thread (`.thread-list li:last-child .thread-item::before`), reset de lista (`.thread-list`), nota discreta `.thread-end-note`, grid de duas colunas `.finished-panels` (breakpoint de 900px já existente, sem novo breakpoint), estilos de `.canon-map-panel`/`.ai-adaptation-panel`/`.completion-metadata`. Nenhum token de cor novo.
  - `frontend/vitest.setup.ts` — adicionado um stub mínimo de `ResizeObserver` (ausente no jsdom), necessário porque o novo teste de `AiAdaptationPanel` monta `DragonPanel` (que já usa `ResizeObserver` em produção, mas nunca havia sido exercitado por um teste antes).
- **Componentes adicionados** (todos em `frontend/components/chronicle/`, puramente apresentacionais): `ChronicleCompletedHeader.tsx`, `CanonMapPanel.tsx`, `AiAdaptationPanel.tsx`, `ThreadSegmentRow.tsx`. Novo utilitário `frontend/lib/format.ts` (`formatDate`, `formatDateTime`, `chronicleStatusLabels`, `turnStatusLabels`).
- **Endpoints reutilizados**: `GET /api/parties/{partyId}/chronicles/{chronicleId}/game` (agora retornando 3 campos adicionais), `GET /api/parties/{partyId}` (para o nome da party no cabeçalho, mesmo padrão já usado por `WrittenView`), `POST .../regenerate` (botão já existente, comportamento inalterado).
- **Endpoints alterados**: nenhum novo endpoint; `GameChronicleDetailResponse` ganhou os campos aditivos `creatorName`, `createdAt`, `completedAt` (sem quebra de compatibilidade — campos adicionados, nenhum removido/renomeado).
- **Decisões tomadas**:
  - O botão "Regenerar história" (regeneração automática da adaptação de IA, já em produção) foi mantido exatamente como está — interpretado como uma feature diferente da futura "mini-história manual" que este console não deveria criar. Decisão confirmada com o utilizador antes da implementação.
  - Testes cobrem apenas os componentes de apresentação puros (sem fetch/router); nenhum teste de integração de página inteira (`GameView`) foi tentado neste console, por não haver precedente de mock de `next/navigation`/`api()` no repositório — decisão confirmada com o utilizador.
  - A nota discreta "Fim da thread original." foi incluída (opcional na especificação).
  - `@username` do autor não foi adicionado à thread por não existir nas respostas `GameSegmentResponse`/`GameTurnResponse` atuais — ver §11 (pergunta em aberto) em vez de inventar o dado.
- **Compatibilidade legada**: nenhuma migração de dados; histórias antigas sem `completedAt` (júnior teoricamente impossível para uma run já `COMPLETED`, mas tratado defensivamente) simplesmente omitem a linha "Concluída em"; `generatedStory` ausente já era um estado suportado e continua sendo tratado por `AiAdaptationPanel` sem quebrar; crônicas arquivadas (`ARCHIVED`) sem história geram uma mensagem honesta em vez de reaproveitar o estado "pendente"/"processando".
- **Riscos encontrados**:
  - O achado do call-site quebrado (ver acima) — já corrigido.
  - Um cache de build (`frontend/.next/`, gitignored) continha um `validator.ts` obsoleto referenciando a rota `frontend/app/dev-preview/page.tsx` (git-ignored, recriada sob demanda pelo comando `/fe-preview`), causando falha do `next build`. Removido com segurança (`.next` é artefato de build descartável, não é trabalho do utilizador) e o build foi refeito com sucesso.
- **Testes adicionados**: `frontend/lib/format.test.ts`, `frontend/components/chronicle/CanonMapPanel.test.tsx`, `frontend/components/chronicle/AiAdaptationPanel.test.tsx`, `frontend/components/chronicle/ThreadSegmentRow.test.tsx`, `frontend/components/chronicle/ChronicleCompletedHeader.test.tsx`.
- **Testes alterados**: nenhum teste pré-existente foi modificado (só o setup global `vitest.setup.ts`, para suportar `ResizeObserver`).
- **Testes executados**: `npm run lint` (sem erros), `npm test` (29/29 testes passaram, incluindo os 5 arquivos novos e os 2 pré-existentes), `npm run build` (compilou e gerou as páginas com sucesso após limpar o cache `.next` obsoleto). Todos executados de fato nesta sessão, não apenas assumidos.
- **Testes não executados**: qualquer teste de backend (Maven/Java CLI proibidos neste console) — a compilação do backend não foi confirmada por build real, apenas por leitura cuidadosa do código; recomenda-se ao utilizador confirmar via IntelliJ (Build → Build Project).
- **Validações manuais executadas**: nenhuma — não foi aberto navegador nem capturado screenshot real nesta sessão.
- **Validações manuais pendentes**: desktop largo, tablet, mobile; modo claro e Arcane Editorial Nocturne; história finalizada com adaptação publicada; sem adaptação (pending/processing/failed); com turnos pulados/expirados; com segmento desabilitado; história legada; história ainda em andamento (confirmar que o layout não mudou). Recomenda-se usar `/fe-preview` para os estados que não dependem de backend real.
- **Estado final**: **Requer validação manual**. Lint, testes automatizados e build de produção passaram de fato; a validação visual em navegador (obrigatória antes de marcar como "Concluída", por regra deste console) ainda não foi executada.

---

### Console nº3 — Reformulação da página da história-jogo finalizada + barra de progresso

- **Data**: 2026-07-30.
- **Estado do working tree no início**: já havia alterações locais não commitadas herdadas de sessões anteriores desta mesma janela (`backend/CLAUDE.md`, `GameChronicleDetailResponse.java`, `GameChronicleService.java`, este arquivo, `frontend/app/app/party/[partyId]/chronicle/[chronicleId]/page.tsx`, `frontend/app/globals.css`, `frontend/lib/types.ts`, `frontend/vitest.setup.ts`, além de `frontend/components/chronicle/`, `frontend/lib/format.ts`/`format.test.ts` não rastreados) — todas preservadas; nenhuma foi descartada ou revertida neste console.
- **Objetivo**: pedido explícito do utilizador com 5 mudanças na página de crônica-jogo e no Arquivo do Cronista: (1) Mapa do cânone acima da Thread original, largura total, colapsável (fechado por padrão), só quando finalizada; (2) história adaptada em modal, com o antigo botão "Regenerar história" virando o botão de abertura do modal, e "Regenerar história" realocado para o canto superior direito do próprio modal; (3) novo botão "Re-executar IA" ao lado do botão de abrir o modal; (4) navegação entre versões anteriores da história (e do mapa, quando existir); (5) barra de progresso de turnos acima do título dos cards em progressão no Arquivo do Cronista e abaixo do título na página da história.
- **Decisões confirmadas com o utilizador antes da implementação** (via `AskUserQuestion`, registradas também em §5 e §11):
  - O Mapa do cânone **continua um placeholder honesto**, sem geração real por IA — decisão explícita do utilizador, não uma limitação técnica. "Re-executar IA" chama exatamente a mesma regeneração de história que "Regenerar história"; um comentário no código (`page.tsx`) documenta essa equivalência temporária.
  - Barra de progresso calculada por **turnos concluídos** (`SUBMITTED`/`SKIPPED`/`EXPIRED`) sobre o **total de turnos atual** (não por pessoas-únicas que completaram todos os ciclos).
  - Navegação de versões é um **seletor inline** (dropdown) dentro do modal, mostrando sempre a versão mais recente por padrão.
- **Arquivos lidos**: `docs/PROJECT_AUDIT.md` e `CLAUDE.md` (raiz) integralmente antes de planejar; depois, leitura direta de `GameChronicleService.java`, `GameChronicleDetailResponse.java`, `ChronicleService.java`, `ChronicleController.java`, `ChronicleCardResponse.java`, `ChronicleEntity.java` (trecho `toCardResponse`), `ChronicleAccessService.java`, `GameTurnRepository.java`, `GameRunRepository.java`, `ChronicleRepository.java`, `GeneratedStoryRepository.java`, `GeneratedStoryEntity.java`, `GeneratedStoryResponse.java`, `GameTurnStatusType.java`, `frontend/app/app/party/[partyId]/chronicle/[chronicleId]/page.tsx`, `frontend/app/app/party/[partyId]/page.tsx`, `frontend/components/chronicle/CanonMapPanel.tsx`(+test), `AiAdaptationPanel.tsx`(+test), `ChronicleCompletedHeader.tsx`(+test), `ThreadSegmentRow.tsx`, `frontend/lib/types.ts`, `frontend/lib/api.ts`, `frontend/lib/format.ts`/`format.test.ts`, `frontend/components/AppShell.tsx` (padrão de modal existente), `frontend/app/globals.css` (regras de modal/collapsible/grid relevantes), `frontend/CLAUDE.md`, `backend/CLAUDE.md`. Duas explorações somente-leitura em paralelo (frontend e backend) precederam o plano; `ExitPlanMode` foi usado e o plano aprovado pelo utilizador antes de qualquer edição.
- **Endpoints adicionados**:
  - `GET /api/parties/{partyId}/chronicles/{chronicleId}/generated-stories` → `List<GeneratedStoryResponse>`, ordenado por versão decrescente (`GameChronicleService.listGeneratedStories`, mesma checagem de acesso/tipo de `detail()`). Nenhuma migration nova — `generated_stories` já era uma tabela versionada (`chronicle_id` + `version_number` UNIQUE), nunca sobrescrita.
- **Endpoints/DTOs alterados**:
  - `ChronicleCardResponse` (`GET /api/parties/{partyId}/chronicles`) ganhou `Integer completedTurns, Integer totalTurns` (aditivo, nullable) — populados só quando `type == GAME && status == IN_PROGRESS`; `null` para os demais casos. Cálculo via nova query agregada `GameTurnRepository.findProgressByChronicleIds` (uma query extra para todos os IDs relevantes, não N+1) e nova projection `ChronicleTurnProgressProjection`. `ChronicleEntity.toCardResponse()` passou a exigir os dois parâmetros (`toCardResponse(Integer, Integer)`); único call-site (`ChronicleService.list()`) foi atualizado.
- **Arquivos de backend alterados/criados**:
  - Criado: `backend/.../chronicle/models/projections/ChronicleTurnProgressProjection.java`.
  - Modificados: `GameTurnRepository.java` (nova query agregada), `GeneratedStoryRepository.java` (novo `findAllByChronicleIdOrderByVersionNumberDesc`), `GameChronicleService.java` (injeta `GeneratedStoryRepository`, novo `listGeneratedStories`), `ChronicleController.java` (nova rota), `ChronicleCardResponse.java`, `ChronicleEntity.java`, `ChronicleService.java` (injeta `GameTurnRepository`, monta o mapa de progresso em `list()`).
- **Arquivos de frontend criados**: `frontend/lib/progress.ts` (+`progress.test.ts`), `frontend/components/chronicle/GameProgressBar.tsx` (+test), `frontend/components/chronicle/StoryModal.tsx` (+test).
- **Arquivos de frontend alterados**:
  - `CanonMapPanel.tsx` (+test) — de `<section>` estático para `<details className="card canon-map-panel">` fechado por padrão.
  - `ChronicleCompletedHeader.tsx` (+test) — novo prop obrigatório `completedTurns`, renderiza `GameProgressBar` abaixo do `<h1>`.
  - `frontend/app/app/party/[partyId]/chronicle/[chronicleId]/page.tsx` (`GameView`) — novo estado `storyModalOpen`; `CanonMapPanel` movido para antes da `<section className="thread">`; removida a seção `.finished-panels` (grid de 2 colunas); `AiAdaptationPanel` deixou de ser importado/renderizado diretamente (só existe dentro de `StoryModal` agora); botão "Regenerar história" no cabeçalho virou dois botões — "Ver história adaptada" (abre o modal, visível sempre que `finished`, não só para o narrador) e "Re-executar IA" (mesma condição de gating que o antigo botão de regenerar); `<GameProgressBar>` adicionado logo abaixo do `<h1>` no cabeçalho em andamento; `<StoryModal>` renderizado no fim do componente. `WrittenView` não foi tocado.
  - `frontend/app/app/party/[partyId]/page.tsx` (Arquivo do Cronista) — `<GameProgressBar>` acima de `.card-heading` para cards `GAME`/`IN_PROGRESS` com `totalTurns` disponível.
  - `frontend/lib/types.ts` — `ChronicleCard` ganhou `completedTurns?`/`totalTurns?` (opcionais, aditivos).
  - `frontend/app/globals.css` — removida `.finished-panels` (grid de 2 colunas) e seu breakpoint de 900px; `.canon-map-panel` virou bloco de largura total com `summary` estilizado; novo `.story-modal` (variante mais larga de `.account-modal`, reaproveitando `.modal-layer`/`.modal-heading`/`.modal-close`); novo `.modal-actions`; novo `.progress-bar`/`.progress-bar-track`/`.progress-bar-fill`/`.progress-bar-label`, reaproveitando tokens de cor já existentes (`--violet-strong`, `--parchment-rgb`, `--muted`) — nenhuma cor nova foi introduzida.
- **Problema encontrado e corrigido durante a implementação**: `StoryModal.tsx` inicialmente usava um segundo `useEffect` para resetar a versão selecionada quando `currentStory?.id` mudava — o ESLint (`react-hooks/set-state-in-effect`) rejeitou esse padrão (`setState` síncrono dentro de efeito). Corrigido usando o padrão recomendado pela documentação do React de ajustar estado durante a renderização (comparação de `lastStoryId` guardado em estado, sem `useEffect`), mantendo apenas o efeito de busca assíncrona das versões (que já seguia o padrão correto, `setState` dentro de um `.then`).
- **Testes adicionados/alterados**: `frontend/lib/progress.test.ts` (novo), `GameProgressBar.test.tsx` (novo), `StoryModal.test.tsx` (novo, mocka `@/lib/api` via `vi.mock`), `CanonMapPanel.test.tsx` (atualizado para o `<details>` fechado por padrão), `ChronicleCompletedHeader.test.tsx` (atualizado para o novo prop `completedTurns` e a query ambígua corrigida — `getByText(/8 turnos/)` colidia com o novo texto da barra de progresso, trocado por `getByText(/8 turnos · 2 ciclos/)`). Nenhum teste de backend foi adicionado (mantém a prática já registrada de cobertura mínima no backend — decisão explícita do plano, não esquecimento).
- **Testes executados de fato nesta sessão**: `npm run lint` (limpo após a correção do `useEffect`), `npm test` (42/42 testes passaram, incluindo os novos), `npm run build` (compilou e gerou as páginas com sucesso).
- **Testes não executados**: Maven/backend — não executado nesta sessão sem perguntar ao utilizador (regra fixa do projeto); leitura cuidadosa do código Java foi usada como substituto. Recomenda-se confirmar via IntelliJ (Build → Build Project).
- **Validação manual**: sandbox `/dev-preview` foi regenerado (`frontend/app/dev-preview/page.tsx`, git-ignored) cobrindo todas as variações (barra de progresso em 0%/parcial/100%/singular, cabeçalho em andamento, cabeçalho finalizado com os novos botões, Mapa do cânone colapsado, modal aberto com seletor de versão via um mock de `fetch` interceptando só a chamada de `/generated-stories`, e o grid do Arquivo do Cronista com um card em progresso). **A extensão Claude in Chrome não estava disponível nesta sessão (o utilizador optou por não instalá-la)**, então a inspeção visual real em navegador **não foi executada** — o utilizador precisa abrir `npm run dev` (já havia um servidor rodando em `http://localhost:3000` antes deste console, iniciado fora desta sessão) e checar `/dev-preview` manualmente antes de considerar a feature validada visualmente.
- **Estado final**: **Requer validação manual**. Lint, testes automatizados e build de produção passaram de fato. Compilação do backend não foi verificada via Maven (regra do projeto). Validação visual em navegador real continua pendente (nem este console nem o Console 2 a executaram).

---

### Console nº4 — Ajustes finos: modal, cards do arquivo, ordenação, bug de regeneração, navegação e logs

- **Data**: 2026-07-30.
- **Objetivo**: 9 ajustes pontuais pedidos pelo utilizador sobre o trabalho do Console nº3, mais 2 pedidos adicionais feitos durante a revisão do plano (refinamento da regra de ordenação com sub-critérios, e logging de backend). Nenhuma migration nova.
- **Achado do utilizador durante este console**: o botão "Regenerar história" (e o novo "Re-executar IA") quebrava com `Unexpected end of JSON input` — bug real, não hipotético; corrigido (ver abaixo).
- **Decisões confirmadas com o utilizador**:
  - Ordenação: pendentes antes de concluídos, sem separador visual; dentro dos pendentes, crônica onde é a vez do usuário logado primeiro, depois mais antiga→mais nova por criação; dentro dos concluídos, mais nova→mais antiga por criação.
  - Logging: o utilizador ofereceu duas opções ("logs em modo debug" ou "perfil local") e delegou a escolha ao assistente — decisão tomada: instrumentar com `@Slf4j` os fluxos de turno/ciclo e da fila de IA, e ligar o nível `DEBUG` apenas no `application-local.yml` (perfil pessoal do desenvolvedor, já git-ignorado), sem tocar `application.yml` (produção continua silenciosa).
- **Backend — alterado**:
  - `ChronicleCardResponse`: campos aditivos `createdAt` (`Instant`, direto de `ChronicleEntity.createdAt`, já existia via `@CreationTimestamp`) e `awaitingCurrentUser` (`Boolean`, nullable, só populado para `GAME`+`IN_PROGRESS`).
  - `ChronicleEntity.toCardResponse(...)`: novo parâmetro `awaitingCurrentUser`.
  - Novo `backend/.../chronicle/models/projections/ActiveTurnUserProjection.java` (`chronicleId`, `userId`) e nova query `GameTurnRepository.findActiveTurnUsersByChronicleIds` (mesmo padrão da já existente `findProgressByChronicleIds`), buscando o usuário do turno `ACTIVE` por crônica.
  - `ChronicleService.list()`: injeta `CurrentUserService`; reaproveita `inProgressGameIds` já calculado para o progresso de turnos; monta o mapa de `awaitingCurrentUser` comparando o usuário do turno ativo com o usuário autenticado.
  - `GameChronicleService`, `AiJobService`, `AiJobStateService`, `AiJobProcessor`, `AiJobWorker`: ganharam `@Slf4j` e logs `debug`/`info`/`warn` nos pontos de negócio mais relevantes (criação/avanço/conclusão de run, varredura de expiração, pedido de regeneração, enfileiramento/claim/complete/fail de job de IA). Nenhum segredo/token é logado.
  - `backend/src/main/resources/application-local.yml` (não versionado): adicionada `logging.level.com.narrativeplatform: DEBUG`.
  - `backend/CLAUDE.md`: nova seção "Logging" documentando o mecanismo.
- **Frontend — corrigido (bug real)**:
  - `frontend/lib/api.ts`: `api()` não assumia mais corretamente corpo vazio para status 2xx diferentes de `204` (ex.: `202 Accepted` sem corpo do endpoint `/regenerate`), causando `response.json()` falhar com `Unexpected end of JSON input`. Corrigido lendo o corpo como texto primeiro e só fazendo `JSON.parse` se não vazio, para qualquer status "ok".
- **Frontend — alterado**:
  - `frontend/app/globals.css`: `.modal-actions` ganhou `flex-wrap: nowrap` a partir de 640px (mantém empilhamento em telas pequenas); `.chronicle-card`/`.chronicle-art` de 185px para 205px de altura; novo `.card-progress-row` (barra de progresso + pill de status lado a lado); `.chronicle-copy p` ganhou `text-overflow: ellipsis` (reforço do clamp de 3 linhas já existente); novo `.nav-group`/`.nav-subitem` (indentação da navegação aninhada).
  - `frontend/app/app/party/[partyId]/page.tsx`: cards com progresso (`GAME`+`IN_PROGRESS`+`totalTurns`) agora mostram a barra e a pill de status juntas em `.card-progress-row` acima do título, em vez da pill ao lado do título; nova função `compareChronicles` e `sortedChronicles` (via `useMemo`) reordenando a lista pela regra descrita acima, usada no lugar do array bruto vindo do backend; link "← Todas as parties" virou "← Grupos"; eyebrow "Arquivo do Cronista" virou "Histórias" (mesma tela, para consistência com o novo nome do menu — a página de login/registro, que também tem um eyebrow "Arquivo do Cronista" como nome de módulo/marca, não foi tocada por ser um contexto diferente, pré-login).
  - `frontend/lib/types.ts`: `ChronicleCard` ganhou `createdAt` (obrigatório, a entidade sempre tem) e `awaitingCurrentUser?` (opcional).
  - `frontend/components/AppShell.tsx`: "Todas as parties" → "Grupos" (com sufixo `` - {nome do grupo}`` quando `partyId` está presente, buscado via novo `useEffect` que chama `GET /parties/{partyId}` e falha silenciosamente); "Arquivo do Cronista" → "Histórias", agora visualmente aninhado (indentado, `.nav-subitem`) sob "Grupos" dentro de um `.nav-group`, tanto no `<nav>` desktop/mobile quanto nos `title` dos botões do `.mobile-rail`. Condição de visibilidade de "Histórias" não mudou (já dependia de `partyId`).
- **Problema de lint encontrado e corrigido durante a implementação**: o novo `useEffect` de busca do nome do grupo em `AppShell.tsx` inicialmente chamava `setPartyName(undefined)` de forma síncrona dentro do próprio corpo do efeito quando `partyId` não existia — o ESLint (`react-hooks/set-state-in-effect`) rejeitou, mesmo padrão de problema já visto no Console nº3 com `StoryModal.tsx`. Corrigido com a mesma técnica (ajuste de estado durante a renderização, comparando um `lastPartyId` guardado em estado, em vez de `useEffect`).
- **Testes alterados**: nenhum teste pré-existente precisou de ajuste além do que os problemas de lint acima já cobriram (nenhum teste cobre `AppShell.tsx` ou a página do Arquivo do Cronista, confirmado novamente neste console). Nenhum teste de backend novo (mantém a prática já registrada; o utilizador foi avisado de que a nova query `findActiveTurnUsersByChronicleIds` e a lógica de ordenação ficaram sem cobertura de backend).
- **Testes executados de fato nesta sessão**: `npm run lint`, `npm test` (42/42) e `npm run build`, todos executados após corrigir os dois problemas acima (o de lint do `useEffect` e um erro de tipo no `/dev-preview` regenerado por causa do novo campo obrigatório `createdAt`).
- **Testes não executados**: Maven/backend, pela mesma regra fixa do projeto (não executar sem perguntar). Leitura cuidadosa do código como substituto.
- **Validação manual**: `/dev-preview` foi regenerado do zero cobrindo os 6 itens visuais desta sessão (modal em linha única em tela larga, cards mais altos, pill ao lado da barra de progresso, clamp de 3 linhas com texto longo, ordem exata esperada dos 5 cards de exemplo, navegação aninhada com e sem nome de grupo). A extensão Claude in Chrome continuou indisponível nesta sessão (mesma limitação do Console nº3) — inspeção visual real em navegador **não foi executada**; o utilizador deve abrir `/dev-preview` manualmente, e também confirmar contra um backend real que o erro de JSON não ocorre mais e que os logs aparecem com `application-local.yml` em `DEBUG`.
- **Estado final**: **Requer validação manual** (mesma ressalva dos consoles anteriores). Lint, testes automatizados e build de produção passaram de fato nesta sessão.

---

### Console nº5 — Correções de UX no StoryModal (cabeçalho, botão de fechar, feedback de regeneração)

- **Data**: 2026-07-30.
- **Objetivo**: 4 correções pontuais no `StoryModal` reportadas pelo utilizador ao usar o modal de verdade (não hipotéticas): alinhamento/espaçamento do cabeçalho; botão de fechar descendo em telas pequenas; os botões de regenerar sumindo sem explicação após clicar; botão "Regenerar história" podendo quebrar linha. Só frontend, nenhuma migration, nenhuma mudança de backend.
- **Achado do utilizador**: depois de clicar em "Regenerar história" (ou "Re-executar IA"), os dois botões desapareciam completamente. Investigação: não é um bug de lógica — o backend corretamente rejeita uma segunda regeneração concorrente (`409`), e o frontend só mostrava os botões quando `status` estava em `PUBLISHED`/`FAILED`, que deixa de ser verdade assim que a regeneração começa (`AI_PENDING`/`AI_PROCESSING`). O problema real era ausência de feedback visual — os botões simplesmente saíam da tela sem nenhuma explicação de que uma geração já estava em andamento.
- **Correção principal**: em vez de esconder os botões durante `AI_PENDING`/`AI_PROCESSING`, agora eles continuam visíveis, porém desabilitados, com rótulo "Gerando..." — tanto o botão "Re-executar IA" no cabeçalho da página quanto "Regenerar história" dentro do modal.
  - `frontend/app/app/party/[partyId]/chronicle/[chronicleId]/page.tsx` (`GameView`): novo `const isRegenerating = data.narrator && ["AI_PENDING", "AI_PROCESSING"].includes(data.status);`, ao lado do já existente `canRegenerate`. Condição do botão "Re-executar IA" passou de `canRegenerate &&` para `(canRegenerate || isRegenerating) &&`, com `disabled={isRegenerating}` e rótulo condicional. Repassado como novo prop `isRegenerating` ao `<StoryModal>`.
  - `frontend/components/chronicle/StoryModal.tsx`: novo prop obrigatório `isRegenerating: boolean` (não dá para derivar só de `status` dentro do modal porque também precisa saber se o usuário é narrador — por isso vem pronto de `GameView`, igual `canRegenerate`). Botão de regenerar agora renderiza quando `canRegenerate || isRegenerating`, desabilitado com "Gerando..." nesse segundo caso.
- **Nota registrada para o futuro (pedido explícito do utilizador — "qualquer coisa relevante")**: a página não faz polling automático enquanto um job de IA está `AI_PENDING`/`AI_PROCESSING` — o estado "Gerando..." só atualiza para "Regenerar história"/"Re-executar IA" de novo quando o usuário disparar alguma ação ou recarregar a página manualmente. Isso já era uma limitação preexistente (não introduzida neste console), mas ficou mais visível agora que o botão permanece na tela em vez de sumir. Não foi corrigida agora (fora de escopo — exigiria polling ou WebSocket); fica como candidata a um console futuro se o utilizador achar a espera confusa na prática.
- **Botão de fechar**: saiu de dentro de `.modal-heading`/`.modal-actions` e passou a ser filho direto de `.story-modal`, posicionado com `position: absolute; top: -16px; right: -16px;` e fundo sólido (`var(--surface)` + `box-shadow`) — fica sempre no canto superior direito, parcialmente para fora da borda do card, independentemente de quantos itens `.modal-actions` tiver ou da largura da tela. Só afeta `.story-modal .modal-close`; o modal de conta em `frontend/components/AppShell.tsx` (que reaproveita a classe genérica `.modal-close` sem estar dentro de `.story-modal`) não foi tocado.
- **Correção de acompanhamento no mesmo console**: a primeira versão desta mudança não funcionou de fato — o utilizador reportou que só um canto do botão aparecia, o resto "cortado" pela borda do modal. Causa: `.story-modal` tinha `overflow-y: auto` (que, por regra do CSS, também vira `auto` no eixo X quando só um eixo é setado), então qualquer coisa posicionada para fora da caixa do próprio `.story-modal` — incluindo o botão de fechar com offset negativo — era recortada. Corrigido movendo o scroll (`padding`, `max-height`, `overflow-y: auto`) para um novo elemento interno `.story-modal-scroll` (envolvendo `.modal-heading` + `AiAdaptationPanel`), deixando `.story-modal` em si (o elemento que o botão de fechar usa como referência de posicionamento) sem overflow algum. Nenhum teste automatizado pega esse tipo de recorte visual — só apareceu com uso real, reforçando por que a validação manual em navegador continua pendente/necessária.
- **Cabeçalho do modal**: `.story-modal .modal-heading` ganhou `align-items: center` (era `flex-start`, herdado do `.modal-heading` genérico) e `margin-bottom: 1.75rem` (mais respiro antes do `AiAdaptationPanel`) — escopado só ao `.story-modal`, não mudou o `.modal-heading` genérico usado pelo modal de conta.
- **Botão "Regenerar história" nunca quebra linha**: `.modal-actions` passou de `flex-wrap: wrap` (com exceção `nowrap` só a partir de 640px) para sempre `flex-wrap: nowrap`. O botão agora tem ícone (`⟲`, `.regenerate-icon`) e rótulo (`.regenerate-label`) em `<span>`s separados, com `title`/`aria-label="Regenerar história"` sempre presentes; abaixo de 520px o rótulo some via CSS (`.regenerate-label { display: none; }`) e só o ícone fica visível, com a dica acessível via `title` (tooltip nativo) e `aria-label`. Isso só foi possível ficar sempre em uma linha porque o botão de fechar saiu do grupo (item acima) e o próprio botão de regenerar agora colapsa antes de faltar espaço.
- **Testes**: `frontend/components/chronicle/StoryModal.test.tsx` — todas as chamadas existentes ganharam o novo prop obrigatório `isRegenerating`; testes novos: botão "Gerando..." desabilitado quando `isRegenerating=true` mesmo com `canRegenerate=false`; estrutura ícone+rótulo dentro do botão; botão de fechar é filho de `.story-modal` mas não de `.modal-heading` (prova estrutural de que saiu do grupo que podia quebrar linha).
- **Testes executados de fato nesta sessão**: `npm run lint`, `npm test` (45/45, incluindo os novos) e `npm run build`, todos passaram.
- **Testes não executados**: Maven/backend não se aplica (nenhuma mudança de backend neste console).
- **Validação manual**: `/dev-preview` regenerado do zero, com 3 cenários do `StoryModal` real (canRegenerate=true normal; isRegenerating=true mostrando "Gerando..." desabilitado; um contêiner de ~380px com `transform` para forçar um containing block e simular tela estreita, já que `.modal-layer` é `position: fixed` e não seria contido por um `<div>` estreito sem esse truque). A extensão Claude in Chrome continuou indisponível nesta sessão — inspeção visual real em navegador **não foi executada**; o utilizador deve abrir `/dev-preview` manualmente.
- **Estado final**: **Requer validação manual** (mesma ressalva dos consoles anteriores). Lint, testes automatizados e build de produção passaram de fato nesta sessão.

---

### Console nº6 — Pipeline de IA após a conclusão

- **Data**: 2026-07-30.
- **Objetivo**: implementar o pipeline de IA assíncrono executado após a conclusão de uma história-jogo, produzindo três artefatos independentes (mapa do cânone com tags configuráveis por party, adaptação literária com prompt melhorado, sinopse curta para o card), cada um com estado próprio, sem risco à conclusão do turno/jogo. Substitui o console anteriormente planejado como "Extração automática de tags" e absorve a antiga "Mini-história manual" (ver §6).
- **Estado do working tree no início**: limpo (`nothing to commit, working tree clean`, branch `teste2`, commit `93eb1bd`).
- **Arquivos lidos**: `CLAUDE.md` (raiz), `backend/CLAUDE.md`, `frontend/CLAUDE.md`, este arquivo integralmente; depois, via 3 agentes Explore em paralelo + leitura direta: `GameChronicleService.java`, `AiJobEntity`/`AiJobService`/`AiJobStateService`/`AiJobProcessor`/`AiJobWorker`/`AiJobRepository`, `ChronicleEntity`/`GeneratedStoryEntity`/`GeneratedStoryRepository`, `OpenAiClient`, `PartyAccessService`/`PartyEntity`/`PartyMemberEntity`/`PartyRoleType`/`MemberStatusType`, `ChronicleAccessService`, `ChronicleController`, `PartyController`, `GlobalExceptionHandler`/hierarquia `DomainException`, `InvitationServiceTest` (estilo de teste), migrations V1-V3, `GameRunRepository`/`GameSegmentRepository`/`ChronicleRepository`, `CanonMapPanel.tsx`(+test)/`AiAdaptationPanel.tsx`/`StoryModal.tsx`/`ChronicleCompletedHeader.tsx`, `GameView` (`page.tsx` da crônica), página da party, `frontend/lib/{types,api,format}.ts`, `frontend/app/globals.css`. Um agente Plan dedicado validou/formalizou a decisão de arquitetura central antes da implementação. Plano aprovado via `ExitPlanMode` antes de qualquer edição.
- **Decisão de arquitetura central**: generalizar a infraestrutura `ai_jobs` existente com um discriminador `job_type` (`STORY_ADAPTATION_GENERATION`/`CANON_MAP_GENERATION`/`STORY_SYNOPSIS_GENERATION`) em vez de criar uma segunda fila — `AiJobRepository`/`AiJobProcessor` continuam como mecânica de fila compartilhada (lock pessimista, sweep agendado, recuperação de jobs travados, limite de 3 tentativas); o que antes estava fundido em `AiJobStateService`/`AiJobWorker` foi extraído para 3 *handlers* por tipo (`AdaptationJobHandler`, `CanonMapJobHandler`, `SynopsisJobHandler`) implementando a interface `AiJobTypeHandler`, com `AiJobStateService` virando um despachante fino. **Regra de independência**: só `AdaptationJobHandler` muta `ChronicleEntity.status` (exatamente como antes); mapa do cânone e sinopse têm status nas próprias entidades (`CanonMapGenerationEntity.status`, `ChronicleSynopsisEntity.status`), nunca tocando o status da adaptação. `completeRun()` passou a criar 3 registros de job (um por tipo) via um novo `AiJobService.enqueueAutomatic(...)` que nunca lança exceção (a diferença de `enqueue`/`enqueueRequested`, que continuam podendo lançar `ConflictException`/503 para o fluxo explícito de regeneração, inalterado).
- **Migration criada**: `V4__ai_job_types_and_canon_pipeline.sql` — altera `ai_jobs` (+`job_type`, backfill via `DEFAULT` depois removido) e `chronicles` (+`synopsis` VARCHAR(240), +`current_synopsis_id` FK deferida via `DO $$`); cria `party_ai_tag_settings` (1 linha por party+categoria, `UNIQUE(party_id, category)`), `chronicle_synopses` (versionada como `generated_stories`), `canon_map_generations` (versionada, com status próprio), `canon_map_generation_categories` (snapshot da config no momento da geração — imutável mesmo se a config da party mudar depois), `canon_tags` (nome normalizado + dedup por `UNIQUE(generation_id, category, normalized_name)`), `canon_tag_sources` (FK real para `game_segments`, nunca duplica o texto do segmento). **Próximo número seguro agora: `V5`.**
- **Backend criado**:
  - Novo bounded context `canon`: enums `CanonCategoryType`/`TagColorType`/`TagBasisType`/`CanonMapStatusType`; entidades `PartyAiTagSettingEntity`/`CanonMapGenerationEntity`/`CanonMapGenerationCategoryEntity`/`CanonTagEntity`/`CanonTagSourceEntity`; repositórios correspondentes (incluindo `findForUpdate` com `@Lock(PESSIMISTIC_WRITE)` em `CanonMapGenerationRepository`, espelhando `AiJobRepository`); `PartyAiTagSettingsService` (GET/PUT, lazy-default dos 5 valores documentados, autorização narrator/owner via `PartyAccessService`, nunca deleta+recria linhas); `CanonMapGenerationService` (snapshot da config + enqueue); `CanonMapValidationService` (validação pura, 9 regras em ordem, rejeição atômica — ver código para lista completa); `CanonMapJobHandler`; `CanonMapValidationException` (interna, nunca exposta via HTTP); `PartyAiTagSettingsController` (`GET`/`PUT /api/parties/{partyId}/ai-tag-settings`).
  - `aijob` estendido: `AiJobType` (enum), `AiJobTypeHandler` (interface), `AdaptationJobHandler` (extração 1:1 do comportamento antigo, prompt revisado para prosa conectiva), `AiRawGenerationResult` (novo tipo de fronteira worker↔state-service). `AiJobEntity`/`AiGenerationCommand`/`AiJobRepository`/`AiJobService` atualizados (ver decisão de arquitetura). `AiJobStateService` reescrito como despachante. `AiJobWorker` simplificado (parsing movido para os handlers).
  - `chronicle` estendido: `SynopsisStatusType`, `ChronicleSynopsisEntity` (espelha `GeneratedStoryEntity`), `ChronicleSynopsisRepository`, `ChronicleSynopsisService`, `SynopsisJobHandler` (em `aijob.services`, delega persistência), `AiArtifactsQueryService` (agrega os 3 artefatos + `aiConfigured`), `AiArtifactsResponse`/`ChronicleSynopsisResponse`. `ChronicleEntity` ganhou `synopsis`/`currentSynopsis`; `toCardResponse` agora prefere `synopsis` sobre `generatedPreview` quando `status == PUBLISHED` e `synopsis != null` (fallback nunca deixa o card vazio). `GameChronicleService.completeRun()` passou a enfileirar os 3 artefatos (mesma transação, só inserts, nenhuma chamada HTTP). `ChronicleController` ganhou `GET /{chronicleId}/ai-artifacts`.
- **Endpoint agregado**: `GET /api/parties/{partyId}/chronicles/{chronicleId}/ai-artifacts` — complementa (não substitui) `GET .../game`; `GameView` continua lendo `data.status`/`data.generatedStory` de `.../game` para `AiAdaptationPanel`/`StoryModal`, sem nenhuma mudança de comportamento. O novo endpoint alimenta só `CanonMapPanel` e expõe `aiConfigured: boolean` (via `OpenAiClient.configured()`) — decisão tomada porque nenhum enum de status persistido (`AiJobStatusType`/`CanonMapStatusType`/`SynopsisStatusType`) tem um valor `AI_NOT_CONFIGURED` (todos ficam `PENDING/PROCESSING/COMPLETED/FAILED`, adaptando aos enums já existentes em vez de inventar um novo estado persistido); o frontend deriva "IA não configurada" como `!aiConfigured && status === "PENDING"`.
- **Prompts**: mapa do cânone (Canon Archivist, JSON estrito por categoria habilitada, regras anti-invenção de fatos + regras de `CREATIVE_FILL` restrito a aparência/personalidade); sinopse (Chronicle Herald, teaser ≤240 caracteres, sem revelar o final); adaptação (mesma estrutura de antes, só a instrução de prosa mudou para pedir transições conectivas explícitas).
- **Frontend**: `CanonMapPanel.tsx` reescrito por completo (busca própria via `ai-artifacts`, todos os estados: pending/processing/failed/IA-não-configurada/desabilitado/vazio/com dados, badges de origem `EXPLICIT`/`INFERRED`/`CREATIVE_FILL`); `CanonMapPanel.test.tsx` reescrito (os 2 testes antigos fixavam a cópia do placeholder, que deixou de existir). Novo `AiTagSettingsPanel.tsx` (+test) — painel colapsável na página da party (mesmo padrão de `.members-panel`/`.editors-panel`, não existe menu de três pontos no projeto), narrator/owner-gated, sem autosave, restaura o último estado confirmado em caso de erro ao salvar. `frontend/lib/types.ts` ganhou `CanonCategory`/`TagColor`/`TagBasis`/`CanonMap`/`ChronicleSynopsis`/`AiArtifacts`/etc. `frontend/lib/format.ts` ganhou `canonCategoryLabels`/`tagBasisLabels`. `globals.css` ganhou 2 tokens novos (`--azure`/`--slate`, + `-rgb`, nos dois temas) e classes novas para seções de categoria/chips/badges/swatches/linhas de configuração — nenhum token existente foi alterado.
- **Achado durante a implementação (bug real, corrigido)**: o primeiro rascunho de `CanonMapJobHandler.buildPrompt` continha um text block Java com o delimitador de abertura `"""` seguido de conteúdo na mesma linha (`.append(""". Never emit...`), o que é sintaticamente inválido em Java (o delimitador de abertura precisa ser seguido por quebra de linha). Corrigido dividindo em dois text blocks separados, cada um com o `"""` de abertura seguido corretamente por uma quebra de linha. Só foi detectado porque `mvn compile` foi executado de fato — reforça por que a compilação real (não só leitura de código) importa.
- **Achado durante a implementação (edição externa concorrente)**: enquanto a IA aguardava resposta do utilizador a uma pergunta sobre autorização de `mvn`, os arquivos `AiJobType.java` e `AiJobWorker.java` foram alterados externamente (pelo utilizador ou por um hook/linter), introduzindo um modelo de IA por tipo de tarefa (`AiJobType.getModelByType()`, com nomes de modelo literais `gpt-5.6-sol`/`gpt-5.6-terra`/`gpt-5.6-luna`) e propagando um parâmetro `model` explícito por `OpenAiClient.generate`/`AiJobWorker.processOne`/`AiJobProcessor`. Essas mudanças foram preservadas (não revertidas) e completadas: `OpenAiClient.generate` tinha ficado inconsistente (retornava `properties.model()` global em vez do `model` efetivamente usado na chamada) — corrigido para retornar o `model` recebido como parâmetro. Ver §11 para a pergunta em aberto sobre se esses nomes de modelo são reais ou temporários.
- **Compatibilidade legada**: jobs históricos de `ai_jobs` recebem `job_type='STORY_ADAPTATION_GENERATION'` via `DEFAULT` da migration (depois removido) — nenhum backfill manual de aplicação. `generated_stories`/versionamento da adaptação inalterados. Crônicas concluídas antes deste console não têm linhas de `canon_map_generations`/`chronicle_synopses` — painéis mostram estado honesto "não gerado", card cai no fallback `generatedPreview` para sempre. Nenhum backfill de crônicas antigas foi tentado. `regenerate()`/"Re-executar IA" continua só-adaptação, sem parâmetro de tipo — não há ação de UI para regenerar mapa/sinopse neste console.
- **Testes adicionados (backend, Mockito puro, mesmo estilo de `InvitationServiceTest` — sem Testcontainers/H2)**: `AiJobServiceTest`, `AiJobStateServiceTest` (independência entre os 3 tipos — um falha, outro conclui, terceiro pendente, sem vazamento de status entre handlers), `CanonMapValidationServiceTest` (13 casos, uma regra de rejeição por teste), `PartyAiTagSettingsServiceTest` (lazy-default, autorização real via `PartyAccessService`, zero-habilitadas válido, nunca recria linhas), `CanonMapGenerationServiceTest` (snapshot imutável — mudar a config depois não afeta gerações já criadas), `GameChronicleServiceTest` (novo — hoje inexistente antes deste console; completar o último turno enfileira os 3 artefatos exatamente uma vez; retry do último turno não duplica, protegido pelo lock pessimista já existente em `GameRunEntity`), `ChronicleEntityTest` (fallback sinopse↔preview no card).
- **Testes adicionados (frontend, Vitest/RTL)**: `CanonMapPanel.test.tsx` (reescrito, todos os estados), `AiTagSettingsPanel.test.tsx` (narrator edita/salva sem autosave, restaura estado confirmado em erro).
- **Testes executados de fato nesta sessão**: frontend — `npm run lint` (limpo, após corrigir um `react-hooks/set-state-in-effect` em `CanonMapPanel.tsx`, mesmo tipo de problema já visto nos Consoles 3/4), `npm test` (54/54 passaram, incluindo os novos), `npm run build` (compilou, gerou `/dev-preview` também). Backend — `mvn compile` e `mvn test-compile` (com autorização explícita do utilizador) passaram de fato, confirmando que todo o código novo compila, incluindo a correção do bug de text block acima.
- **Testes não executados**: `mvn test` (autorizado pelo utilizador, mas falhou por um problema de ambiente pré-existente e não relacionado a este console — ver §11 para detalhes do mirror Maven privado inacessível). Os testes de backend acima foram escritos e revisados manualmente com cuidado, e o projeto compila com eles, mas **a execução real da suíte JUnit não foi confirmada nesta sessão**.
- **Validações manuais executadas**: nenhuma em navegador real (mesma limitação dos consoles anteriores — Claude in Chrome não usado nesta sessão). `/dev-preview` foi regenerado cobrindo todos os estados de `CanonMapPanel` (pending/processing/failed/IA-não-configurada/desabilitado/vazio/com dados) e do `AiTagSettingsPanel` (narrador com sucesso, narrador com falha simulada, jogador comum sem o painel).
- **Validações pendentes**: abrir `/dev-preview` num navegador real (claro e `Arcane Editorial Nocturne`); confirmar `mvn test` com sucesso num ambiente com acesso ao mirror Maven correto (ex.: IntelliJ); nenhuma chamada real à OpenAI foi feita — a integração de IA não foi validada ponta a ponta.
- **Estado final**: **Requer validação manual e execução de testes**. Compilação do backend confirmada de fato (`mvn compile`/`test-compile`); execução da suíte de testes de backend bloqueada por ambiente (não por código); frontend com lint/testes/build passando de fato. Não é seguro afirmar "concluído" sem essas confirmações pendentes.

---

### Console nº7 — Sete ajustes de UX/UI: participação por crônica, timeload de IA, re-run divergente e página de backend fora do ar

- **Data**: 2026-07-31.
- **Estado do working tree no início**: limpo, exceto `docs/dwarfAsciiArt.txt` (não rastreado, fornecido pelo utilizador nesta sessão para a página de backend fora do ar).
- **Objetivo**: 7 mudanças pontuais pedidas pelo utilizador, sem relação direta com o roadmap de 19 itens (§6): (1) botão "Registrar crônica" nunca quebra linha, colapsa para ícone com tooltip em telas estreitas; (2) qualquer membro pode sair das próximas cycles de uma crônica-jogo específica (ou o narrador remover a si mesmo/outro), preservando os trechos já escritos; (3) botão de reingresso substituindo o de saída, com a regra de que só um narrador pode trazer de volta quem foi removido por um narrador (auto-removidos podem voltar sozinhos); (4) timeload que acompanha o processamento de IA (sinopse + mapa canônico + adaptação) e para quando todos os artefatos disparados terminam; (5) "Re-executar IA" passou a disparar os 3 pipelines (pulando os que já estão ativos), enquanto "Regenerar história" continua só-adaptação; (6) nome do grupo truncado com "..." + tooltip no menu "Grupos"; (7) página `/backend-down` com arte ASCII de anão, e detecção global de falha de rede em `lib/api.ts` redirecionando para ela.
- **Arquivos lidos integralmente antes de planejar**: `CLAUDE.md` (raiz), `backend/CLAUDE.md`, `frontend/CLAUDE.md`, este arquivo (`docs/PROJECT_AUDIT.md`) integralmente — depois, 3 agentes Explore em paralelo (modelo de ciclos/participação, pipeline de IA, menu/tema/tratamento de erro) e 1 agente Plan, seguidos de leitura direta de `GameChronicleService.java`, `ChronicleController.java`, `ChronicleAccessService.java`, `GameTurnEntity.java`, `GameRunEntity.java`, `GameChronicleDetailResponse.java`, `AiJobService.java`, `CanonMapGenerationService.java`, `ChronicleSynopsisService.java`, `PartyService.java`, `PartyAccessService.java`, `frontend/lib/api.ts`, `frontend/lib/types.ts`, `frontend/components/AppShell.tsx`, `frontend/app/app/party/[partyId]/page.tsx`, `frontend/app/app/party/[partyId]/chronicle/[chronicleId]/page.tsx`, `frontend/components/chronicle/{StoryModal,CanonMapPanel}.tsx`, `frontend/app/globals.css` (trechos relevantes), migrations V1-V4. Plano revisado com o utilizador via `AskUserQuestion` (4 decisões, ver abaixo) antes de `ExitPlanMode`.
- **Decisões confirmadas com o utilizador antes da implementação**:
  - Intervalo de polling do timeload: **5 segundos**.
  - Aviso para quem saiu de uma crônica: texto temático ("caçador de recompensas prendeu seus braços"), com variação por quem removeu (pede para o narrador soltar / pode se soltar sozinho clicando em "Voltar para a história").
  - "Re-executar IA": para cada um dos 3 pipelines, só (re)enfileira os que **não** estão pendentes/processando; os já ativos são ignorados silenciosamente (sem erro).
  - Detecção de "backend fora do ar": **global**, em toda chamada `api()` (não só no mount do `AppShell`).
- **Migration criada**: `V5__game_participants.sql` — cria `game_participants` (`run_id`, `user_id`, `status` ACTIVE/LEFT, `removed_by_type` SELF/NARRATOR nullable, `removed_by_user_id` nullable, `left_at` nullable), com backfill de uma linha `ACTIVE` por usuário que já tem turno em cada run existente. Referencia `run_id`/`user_id` (nunca `sequence_number`), respeitando a restrição de design já registrada em §9. **Próximo número seguro agora: `V6`.**
- **Backend criado**: `chronicle/models/enums/{GameParticipantStatusType,RemovedByType}.java`; `chronicle/models/entities/GameParticipantEntity.java` (com `markLeft`/`markRejoined`/`toResponse`); `chronicle/repositories/GameParticipantRepository.java`; `chronicle/models/responses/GameParticipantResponse.java`.
- **Backend alterado**:
  - `GameChronicleService.java`: `create()` agora grava um `GameParticipantEntity` por usuário; `insertIntoRun`/`removeFromRun` (privados) passaram a fazer upsert do participante (`ACTIVE`/`LEFT` + quem removeu), `removeFromRun` ganhou parâmetros `RemovedByType`/`UserEntity actor`; `removePartyMemberFromActiveRuns` ganhou parâmetro `actor`; novos métodos públicos `leaveChronicle`/`rejoinChronicle` (self-service, por crônica, com as regras de autorização acima) e `reRunAi` (dispara os 3 pipelines, pulando os já ativos); `detail()` agora retorna `participants` na resposta.
  - `GameChronicleDetailResponse.java`: campo aditivo `List<GameParticipantResponse> participants`.
  - `PartyService.java`: `removeMember` passa a capturar a membership do narrador atuante e repassá-la como `actor` para `removePartyMemberFromActiveRuns`.
  - `AiJobService.java`: novos métodos `requireConfigured()` (wrapper público de `openAiClient.requireConfigured()`) e `enqueueIfIdle(chronicle, requestedBy, jobType)` (variante de `enqueueInternal` que retorna `false` em vez de lançar `ConflictException` quando já há um job ativo daquele tipo).
  - `CanonMapGenerationService.java`/`ChronicleSynopsisService.java`: novo método `enqueueGenerationIfIdle(chronicle)` em cada um, que só cria uma nova versão/enfileira se a mais recente não estiver `PENDING`/`PROCESSING` (evita criar uma versão órfã sem job real quando já há uma geração em andamento).
  - `ChronicleController.java`: 3 endpoints novos — `POST .../game/participants/{userId}/leave`, `POST .../game/participants/{userId}/rejoin`, `POST .../re-run-ai`.
- **Endpoints adicionados**: `POST /api/parties/{partyId}/chronicles/{chronicleId}/game/participants/{userId}/leave` (204), `POST .../game/participants/{userId}/rejoin` (204), `POST .../re-run-ai` (202). `POST .../regenerate` não foi alterado (continua só-adaptação, com o mesmo contrato de conflito).
- **Frontend criado**: `frontend/components/chronicle/ParticipantsPanel.tsx` (lista participantes, botões "Sair da história"/"Remover da história" em `.button.danger-outline` e "Voltar para a história" em `.button.secondary`, com as mesmas regras de autorização do backend replicadas no client para exibição); `frontend/components/chronicle/AiProcessingTimer.tsx` (pill "Gerando IA... m:ss"); `frontend/lib/useAiArtifacts.ts` (hook de polling a cada 5s enquanto adaptação/mapa/sinopse estiverem pendentes/processando); `frontend/app/backend-down/page.tsx` (página temática com a arte ASCII de `docs/dwarfAsciiArt.txt`, embutida como array de strings para evitar colisão com as crases/aspas do próprio desenho).
- **Frontend alterado**:
  - `frontend/lib/api.ts`: nova classe `NetworkError`; `fetch()` agora é envolvido em `try/catch` — uma falha de rede (não uma resposta HTTP de erro) redireciona a página inteira para `/backend-down` (guardado contra loop se já estiver nela) e lança `NetworkError`.
  - `frontend/components/AppShell.tsx`: efeito de `/auth/me` agora distingue `NetworkError` (não desloga/redireciona para `/`, já que `api()` cuidou do redirecionamento) de qualquer outra falha (comportamento antigo, desloga); label "Grupos" no `<nav>` desktop ganhou `title` + `<span className="nav-label-text">` truncado por CSS.
  - `frontend/app/app/party/[partyId]/page.tsx`: botão "Registrar crônica" ganhou ícone+rótulo em spans separados (`.registrar-cronica-button`), rótulo escondido por CSS abaixo de 480px.
  - `frontend/app/app/party/[partyId]/chronicle/[chronicleId]/page.tsx` (`GameView`): novo `refreshToken` incrementado em toda `action()` bem-sucedida (reinicia o polling); `useAiArtifacts` alimenta `CanonMapPanel` (que deixou de buscar `/ai-artifacts` sozinho) e `AiProcessingTimer`; efeito de borda (`processing` true→false) recarrega os dados uma vez ao final do processamento; `ParticipantsPanel` e o banner temático renderizados só enquanto `status === IN_PROGRESS`; botão "Re-executar IA" do cabeçalho passou a chamar `/re-run-ai` (o botão "Regenerar história" dentro do `StoryModal` continua chamando `/regenerate`, sem alteração).
  - `frontend/components/chronicle/CanonMapPanel.tsx` (+test): parou de fazer fetch próprio — agora recebe `artifacts?: AiArtifacts` como prop (fonte única de polling em `GameView`).
  - `frontend/lib/types.ts`: `AiArtifacts` ganhou o campo `adaptation` (já existia no backend, faltava no tipo do frontend); novos tipos `GameParticipantStatus`/`RemovedByType`/`GameParticipant`; `GameDetail` ganhou `participants: GameParticipant[]`.
  - `frontend/app/globals.css`: `.nav-label-text` (ellipsis, mesmo padrão de `.invite-members-url`), `.registrar-cronica-button` (mesmo padrão de `.regenerate-button`), `.member-left` (reaproveita o visual esmaecido de `.member-removed`), `.ai-processing-timer` (pill), `.backend-down-panel`/`.backend-down-art-frame`/`.backend-down-art` (moldura com `overflow-x: auto` para a arte ASCII). Nenhum token de cor novo — `--danger`/`--danger-rgb` já existentes cobrem o tema "avermelhado" pedido para os botões de saída/remoção.
- **Backend — teste existente ajustado**: `GameChronicleServiceTest.java` ganhou o mock `GameParticipantRepository` e o parâmetro correspondente na construção de `GameChronicleService` (a assinatura do construtor mudou com o novo campo). Nenhum teste novo de backend foi escrito para `leaveChronicle`/`rejoinChronicle`/`reRunAi` neste console — fica registrado como pendência (ver abaixo).
- **Testes executados de fato nesta sessão**:
  - Frontend: `npm run lint` (limpo, após corrigir duas violações reais de `react-hooks/set-state-in-effect`/`react-hooks/purity` em `AiProcessingTimer.tsx` e em `GameView` — a captura de `Date.now()` teve que ser movida para dentro de `useEffect`, disparada via `window.setTimeout`/`setInterval`, em vez de comparação de estado durante a renderização), `npm test` (54/54 passaram, incluindo `CanonMapPanel.test.tsx` reescrito para o novo contrato de props), `npm run build` (compilou, gerou `/backend-down` como rota estática).
  - Backend: com autorização explícita do utilizador, rodado o toolchain portátil (`jdk-21.0.8` + `mvn-3.9.11`) — `mvn compile` (sucesso), `mvn test-compile` (sucesso), `mvn test`: **51 testes, 3 falhas**, todas em `AiJobStateServiceTest` (`claimOnlyDelegatesToTheMatchingHandler`, `completeDelegatesOnlyToTheOwningHandlerAndLeavesOthersUntouched`, `completeRoutesCanonMapValidationFailureThroughTheNormalFailurePath`, erro Mockito `NoInteractionsWanted` no mock `adaptationHandler`). **Confirmado que este console não tocou `AiJobStateService.java` nem `AiJobStateServiceTest.java`** (verificado via `git status --porcelain` — nenhum dos dois aparece como alterado) — a falha é pré-existente e não relacionada às mudanças deste console; é a primeira vez que `mvn test` conseguiu de fato rodar neste ambiente (Console 6 não conseguiu por causa do mirror Maven, ver §11). `GameChronicleServiceTest` (2/2), `CanonMapGenerationServiceTest` (3/3), `PartyAiTagSettingsServiceTest` (7/7), `AiJobServiceTest` (5/5), `InvitationServiceTest` (11/11) e os demais passaram normalmente.
- **Validações manuais executadas**: nenhuma em navegador real (mesma limitação de todos os consoles anteriores — Claude in Chrome não foi usado nesta sessão). `/dev-preview` foi regenerado cobrindo os 6 itens visuais desta sessão (botão "Registrar crônica" em duas larguras, menu "Grupos" com nome longo truncado, `ParticipantsPanel` com narrador e sem narrador, banner temático nas duas variantes de remoção, `AiProcessingTimer` com toggle manual, e a página `/backend-down` embutida).
- **Validações pendentes**: abrir `/dev-preview` num navegador real (claro e escuro) e confirmar visualmente os 6 itens; testar contra um backend real que uma queda de rede de fato redireciona para `/backend-down` sem deslogar o usuário; confirmar que o polling de 5s do timeload realmente para quando os artefatos terminam (requer `OPENAI_API_KEY` configurada ou observação do estado `PENDING` permanente); escrever testes de backend para `leaveChronicle`/`rejoinChronicle`/`reRunAi` (nenhum foi adicionado neste console); investigar a causa raiz das 3 falhas pré-existentes em `AiJobStateServiceTest` (fora do escopo deste console, mas agora visível pela primeira vez porque `mvn test` finalmente conseguiu rodar).
- **Estado final**: **Requer validação manual e cobertura de teste adicional no backend**. Lint, testes automatizados de frontend e build de produção passaram de fato. Backend compila e os testes que tocam código deste console passam; a suíte tem 3 falhas pré-existentes e não relacionadas, confirmadas via `git status`.

---

### Console nº8 — Polish sobre o Console nº7 + categorias dinâmicas do Mapa do cânone

- **Data**: 2026-07-31.
- **Estado do working tree no início**: limpo (continuação do Console nº7 na mesma sessão, já commitável).
- **Objetivo**: duas frentes pedidas pelo utilizador após revisar visualmente o Console nº7. **Parte A** (polish, só frontend): (1) centralizar a arte ASCII do anão em `/backend-down` (estava alinhada à esquerda); (2) quebrar o texto do dragão em dois parágrafos; (3) restringir `ParticipantsPanel` a narradores, dando a jogadores comuns um botão alternante no cabeçalho ("Sair da história"/"Voltar para a história", conforme o status); (4) dar ao aviso de quem saiu da história ("card action-message") uma cor mais chamativa (cobre/laranja) e mais espaço abaixo, já que antes reaproveitava indevidamente a classe da mensagem genérica de erro. **Parte B** (arquitetural, backend+frontend): substituir as 5 categorias fixas do Mapa do cânone (enum `CanonCategoryType`, cor por enum `TagColorType`, checkboxes de habilitar) por categorias totalmente dinâmicas por party — o narrador cria/edita/exclui/reordena categorias com nome livre, descrição livre e cor em hex (seletor nativo do navegador), sem checkbox, com salvamento imediato por linha (sem botão global "Salvar configurações"). A regra/texto de que mudanças só valem a partir da próxima geração de tags foi mantida.
- **Decisões confirmadas com o utilizador antes da implementação**:
  - Parties existentes: as categorias que estavam `enabled=true` no modelo antigo foram convertidas em categorias dinâmicas equivalentes (mesmo nome PT-BR, cor hex aproximada da cor nomeada anterior); parties novas passam a começar **sem nenhuma categoria** (a antiga lógica de "lazy-default das 5 categorias documentadas", registrada como decisão intencional no Console nº6, foi conscientemente substituída, não regredida por acidente).
  - Reordenar categorias: **setas ↑/↓** por linha, não drag-and-drop — não havia biblioteca nem precedente de arrastar-e-soltar em nenhum lugar do frontend; a alternativa mais simples e robusta foi escolhida deliberadamente.
- **Arquivos lidos antes de planejar**: `docs/PROJECT_AUDIT.md` integralmente (mesma sessão, incluindo a entrada recém-criada do Console nº7), `CLAUDE.md`/`backend/CLAUDE.md`/`frontend/CLAUDE.md`; 2 agentes Explore em paralelo (arquitetura backend do enum de categorias — `CanonCategoryType`/`TagColorType`/`PartyAiTagSettingsService`/`CanonMapGenerationService`/`CanonMapJobHandler`/`CanonMapValidationService`/migration `V4`; e arquitetura frontend — `AiTagSettingsPanel.tsx`/`CanonMapPanel.tsx`/`types.ts`/`format.ts`/`globals.css`, confirmando ausência de qualquer biblioteca ou precedente de drag-and-drop); 1 agente Plan; depois leitura direta de `CanonMapValidationService.java`, `CanonMapJobHandler.java`, `PartyAiTagSettingsService.java`, `CanonMapGenerationService.java`, `CanonTagEntity.java`, `CanonMapGenerationCategoryEntity.java`, `PartyAiTagSettingEntity.java`, DTOs/controller/repositórios do módulo `canon`, e os valores hex exatos dos tokens de cor (`--violet`/`--gold`/`--copper`/`--azure`/`--green`/`--danger`/`--slate`) em `globals.css` para o backfill da migration.
- **Migration criada**: `V6__dynamic_canon_categories.sql` — ver §8 para o detalhamento tabela a tabela. **Próximo número seguro agora: `V7`.**
- **Backend criado**: `CanonCategoryEntity` (substitui `PartyAiTagSettingEntity`), `CanonCategoryRepository`, `CanonCategoryService` (substitui `PartyAiTagSettingsService` — CRUD completo: `list`/`create`/`update`/`delete`/`moveUp`/`moveDown`, sem seed automático), `CanonCategoryController` (substitui `PartyAiTagSettingsController`, mesmo path `/api/parties/{partyId}/ai-tag-settings`), `CreateCanonCategoryRequest`/`UpdateCanonCategoryRequest`/`CanonCategoryConfigResponse`.
- **Backend alterado**: `CanonMapGenerationCategoryEntity` (ganhou `name`/`description`, `color` virou `String`, perdeu `category` enum e `enabled` — mecanismo de snapshot imutável por valor preservado sem mudança estrutural, só de formato dos campos); `CanonTagEntity` (perdeu `category`/`color` enum, ganhou `@ManyToOne categorySnapshot` — a cor de uma tag agora é sempre lida via a categoria, nunca mais duplicada); `CanonTagRepository` (query de ordenação trocada para `findAllByGenerationIdOrderByCategorySnapshotDisplayOrderAscDisplayOrderAsc`); `CanonMapGenerationService` (`enqueueGeneration` lê `CanonCategoryRepository` em vez de `getOrCreateDefaults`, tolera lista vazia; `getForChronicle` agrupa tags pelo id da categoria-snapshot em vez do enum); `CanonMapJobHandler.buildPrompt()` (usa `getName()` em vez de `Enum::name`, sanitiza o nome livre antes de embutir no prompt contra caracteres de controle, e usa uma instrução explícita diferente quando não há nenhuma categoria configurada); `CanonMapJobHandler.onCompleted()`/`CanonMapValidationService.validate()` (a checagem `CanonCategoryType.valueOf(fieldName)` virou uma checagem de pertencimento a um `Set<String>` de nomes normalizados — `normalize()` foi promovido a `public static` para ser compartilhado entre as duas classes; as ~12 regras de validação originais mantiveram o comportamento, só o tipo do parâmetro `category` mudou de enum para `String`).
- **Backend apagado**: `CanonCategoryType.java`, `TagColorType.java`, `PartyAiTagSettingEntity.java`, `PartyAiTagSettingRepository.java`, `PartyAiTagSettingsService.java`, `PartyAiTagSettingsController.java`, `TagSettingItemRequest.java`, `UpdateAiTagSettingsRequest.java`, `TagSettingResponseItem.java`, `PartyAiTagSettingsResponse.java`. `TagBasisType` não foi tocado (não relacionado a categorias).
- **Endpoints alterados**: `GET /api/parties/{partyId}/ai-tag-settings` agora devolve `List<CanonCategoryConfigResponse>` direto (sem wrapper `{settings:[...]}`); `PUT` (bulk, exigia exatamente 5 categorias) foi removido. **Endpoints novos**: `POST /api/parties/{partyId}/ai-tag-settings` (criar), `PUT .../ai-tag-settings/{categoryId}` (editar uma), `DELETE .../ai-tag-settings/{categoryId}`, `POST .../ai-tag-settings/{categoryId}/move-up`, `POST .../ai-tag-settings/{categoryId}/move-down`.
- **Frontend alterado**:
  - `frontend/lib/types.ts`: `CanonCategory` deixou de ser uma union fechada de 5 valores e virou uma interface (`id`, `name`, `description?`, `color` hex `string`, `displayOrder`); `TagColor` (union de 7 cores nomeadas) e `TagSetting` foram removidos; `CanonCategorySection` acompanhou a mesma forma.
  - `frontend/lib/format.ts`: `canonCategoryLabels` removido (o nome da categoria agora vem sempre do próprio dado, não de uma tabela de tradução de enum).
  - `frontend/components/chronicle/AiTagSettingsPanel.tsx`: reescrita completa — botão "Adicionar categoria" abre uma linha editável (nome, descrição, `<input type="color">`); cada linha salva mostra nome/descrição/swatch de cor + botões de ícone (↑/↓/editar/excluir, no padrão já estabelecido por `.invite-icon-button`); toda ação persiste imediatamente (sem "Salvar configurações" global); erro de uma linha aparece só naquela linha, sem descartar as demais; texto fixo sobre "só vale para novas gerações" preservado literalmente; estado de lista vazia tratado.
  - `frontend/components/chronicle/CanonMapPanel.tsx`: removido o filtro por `enabled` (toda categoria retornada já está em uso); swatch de cor passou de classe CSS por nome (`canon-color-swatch-${cor}`) para `style={{background: categoria.color}}` inline; rótulo da categoria passou de `canonCategoryLabels[...]` para `categoria.name`; mensagem de "categoria desativada" reescrita para "nenhuma categoria configurada".
  - `frontend/app/globals.css`: removidas as 7 regras `.canon-color-swatch-{gold,copper,violet,azure,green,rose,slate}` (os tokens de cor em si permanecem, usados em outras partes do tema); `.tag-setting-row`/`.tag-setting-name`/`.tag-setting-color`/`.tag-setting-order` substituídas por `.category-row`/`.category-row-info`/`.category-row-actions`/`.category-row-edit`; novo `input[type="color"]` com tamanho 34×34 (mesma convenção de `.invite-icon-button`); `.backend-down-art-frame` ganhou `text-align: center` (removido `min-width:100%`/`text-align:left` que forçavam a arte para a esquerda); nova `.chronicle-left-notice` (fundo `--copper` tingido + borda esquerda, no padrão já existente de `.lock-banner`, com mais margem inferior).
  - `frontend/app/backend-down/page.tsx`: texto do dragão dividido em dois `<p>`.
  - `frontend/app/app/party/[partyId]/chronicle/[chronicleId]/page.tsx` (`GameView`): `ParticipantsPanel` agora só renderiza para `data.narrator`; novo botão alternante em `headerActions` para não-narradores (mesmas classes/endpoints/textos de confirmação já usados por `ParticipantsPanel`); banner de "saiu da história" trocou de `className="card action-message"` para `className="chronicle-left-notice"`.
- **Backend — testes reescritos**: `PartyAiTagSettingsServiceTest` → `CanonCategoryServiceTest` (gate de narrador em create/update/delete/move, `list` liberado para qualquer membro ativo, `create` sempre no fim da ordem, `delete` tolera buracos em `displayOrder`, `moveUp`/`moveDown` trocam com a linha adjacente e são no-op na borda da lista, acesso a categoria de outra party rejeitado como `NotFoundException`); `CanonMapGenerationServiceTest` (mantido o teste de imutabilidade do snapshot — editar a categoria viva depois de gerar não afeta uma geração já feita — mais um caso novo de geração com zero categorias configuradas); `CanonMapValidationServiceTest` (mesmas ~12 regras com `Set<String>`, mais um caso novo de correspondência de nome de categoria insensível a maiúsculas/espaços).
- **Frontend — testes reescritos/ajustados**: `AiTagSettingsPanel.test.tsx` (reescrita completa: carregar lista, estado vazio, adicionar com POST imediato e cancelar sem chamada, editar pré-preenchendo e salvando via PUT, excluir com confirmação, setas de reordenar desabilitadas nas pontas da lista, erro de uma linha não descarta o rascunho, texto fixo da regra preservado, gate de narrador); `CanonMapPanel.test.tsx` (ajustado o teste antigo de "categoria desativada" para dois casos novos — lista vazia de categorias vs. categorias existentes sem elementos identificados — e o teste de renderização de tag chip atualizado para o novo formato `id`/`name`/cor hex).
- **Testes executados de fato nesta sessão**:
  - Frontend: `npm run lint` (limpo), `npm test` (62/62 passaram, incluindo os arquivos reescritos), `npm run build` (compilou, gerou `/backend-down`/`/dev-preview` como rotas estáticas).
  - Backend: com autorização explícita do utilizador (pedida novamente nesta rodada, conforme a regra fixa do projeto de sempre perguntar antes de `mvn`/`java`), toolchain portátil `jdk-21.0.8`+`mvn-3.9.11` — `mvn compile` (sucesso), `mvn test-compile` (sucesso), `mvn test`: **56 testes, 3 falhas**, todas de novo em `AiJobStateServiceTest` (mesmas 3 falhas pré-existentes já registradas no Console nº7 — confirmado que este console não tocou `AiJobStateService.java`/`AiJobStateServiceTest.java`). Todos os testes do módulo `canon` passaram: `CanonCategoryServiceTest` (10/10), `CanonMapGenerationServiceTest` (4/4), `CanonMapValidationServiceTest` (14/14); `GameChronicleServiceTest` (2/2) e os demais inalterados também passaram.
- **Validações manuais executadas**: nenhuma em navegador real (mesma limitação de todos os consoles anteriores). `/dev-preview` foi regenerado cobrindo: arte do anão centralizada e texto em dois parágrafos, painel de participantes só para narrador vs. botão alternante nos 3 estados para jogador, o aviso `chronicle-left-notice` nas duas variantes, e o painel de categorias dinâmicas com um `fetch()` simulado em memória (não é `api()` real, é uma interceptação local só para a prévia) que permite testar de verdade adicionar/editar/excluir/reordenar sem backend.
- **Validações pendentes**: abrir `/dev-preview` num navegador real (claro e escuro) e confirmar visualmente todos os itens acima; testar contra um backend real a migration `V6` (a ordem de passos foi revisada com cuidado mas nunca foi executada de fato contra um banco Postgres real nesta sessão — recomenda-se fortemente rodá-la primeiro num ambiente de teste antes de produção, dado que envolve `DROP TABLE`/`DROP COLUMN` irreversíveis); confirmar que a extração de tags por IA continua funcionando ponta a ponta com nomes de categoria livres (nenhuma chamada real à OpenAI foi feita nesta sessão); investigar a causa raiz das 3 falhas pré-existentes em `AiJobStateServiceTest` (ainda não resolvidas, seguem carregadas de consoles anteriores).
- **Estado final**: **Requer validação manual e execução real da migration `V6` num ambiente de teste antes de produção**. Lint, testes automatizados de frontend e build de produção passaram de fato. Backend compila e todos os testes tocados por este console passam; a suíte mantém as mesmas 3 falhas pré-existentes e não relacionadas de sempre.

---

### Console nº9 — Papel de Espectador, convite de espectador, e aviso do próximo turno por WhatsApp

- **Data**: 2026-07-31.
- **Estado do working tree no início**: limpo (continuação da mesma sessão dos Consoles nº7/nº8, já commitados/enviados).
- **Objetivo**: três pedidos novos e independentes do utilizador: (1) um 4º papel de party, **Espectador**, que só visualiza histórias e nunca recebe turnos em uma história-jogo, ativado/revertido por um botão no painel "Gerenciar membros" (só o dono, mesma regra dos botões de papel já existentes); revertê-lo para jogador devolve automaticamente a pessoa às histórias em andamento; (2) um segundo link de convite reutilizável, paralelo ao de jogador, que registra quem aceita diretamente como Espectador — painel de convite espelhado por completo (copiar/regenerar/WhatsApp), com texto temático de "incumbência de observador" diferente do convite de jogador; (3) um botão "Publicar, passar e avisar o próximo" ao lado de "Publicar e passar" no bloco "É a sua vez", que publica normalmente e depois abre o WhatsApp com uma mensagem citando quem joga em seguida — não aparece no último turno da história.
- **Decisões confirmadas com o utilizador antes da implementação**:
  - Convite de espectador: painel completo espelhado (copiar + regenerar + WhatsApp), não só um botão de copiar.
  - Só o dono da party pode clicar em "Tornar espectador" (mesma regra de "Tornar narrador"/"Tornar jogador" já existente) — confirmado explicitamente, e o utilizador reforçou que a conversão precisa ser **reversível** (o dono também pode tornar um espectador jogador de novo, o que já devolve a pessoa às histórias em andamento).
- **Arquivos lidos antes de planejar**: `docs/PROJECT_AUDIT.md` integralmente (mesma sessão), `backend/CLAUDE.md`/`frontend/CLAUDE.md`; 1 agente Explore (papéis de party, `GameChronicleService.create`/`insertIntoRun`/`insertPartyMemberIntoActiveRuns`, fluxo de convite/aceite, migrations `party_members`/`party_invitation_links`); depois leitura direta de `PartyService.java`, `InvitationService.java`, `InvitationController.java`, `PartyInvitationLinkEntity.java`, `PartyInvitationLinkRepository.java`, `PartyMemberRepository.java`, `PartyController.java`, `InvitePreviewResponse.java`, `frontend/app/invite/[token]/page.tsx`, e o bloco "É a sua vez" da página de crônica.
- **Migration criada**: `V7__party_spectator_invitation_links.sql` — ver §8 para detalhamento. **Próximo número seguro agora: `V8`.** Nenhuma migration foi necessária para `party_members.role` — a coluna já era `VARCHAR(30)` sem `CHECK`, então `SPECTATOR` é puramente uma adição no enum Java.
- **Backend alterado**:
  - `PartyRoleType.java`: novo valor `SPECTATOR` (nenhum `switch`/exhaustiveness quebrou — todo uso no backend já era via `==`/`canNarrate()`).
  - `PartyMemberRepository.java`: novo `findAllByPartyIdAndStatusAndRoleNotOrderByJoinedAtAsc`.
  - `GameChronicleService.java`: `create(...)` agora usa essa query nova (exclui espectadores da seleção de participantes) e lança `BadRequestException` se quem está criando a história-jogo for espectador (sem essa checagem, o criador entraria como turno 0 mesmo excluído da lista); `insertIntoRun(...)` (privado) ganhou um guard no início — busca a membership do usuário na party e vira no-op silencioso se o papel for `SPECTATOR`, protegendo de uma vez só os 3 chamadores existentes (`insertPartyMemberIntoActiveRuns`, aceite de convite, reingresso por crônica específica).
  - `PartyService.updateMemberRole(...)`: guarda o papel anterior antes de trocar; se o novo papel é `SPECTATOR`, chama `removePartyMemberFromActiveRuns` (mesmo mecanismo de sair de crônicas já existente, tira a pessoa dos turnos ainda não jogados preservando o que já escreveu); se o papel anterior era `SPECTATOR` e o novo não é, chama `insertPartyMemberIntoActiveRuns` (mesmo mecanismo de reativação, devolve a pessoa ao fim dos ciclos restantes). Nenhuma lógica de participação nova — só a chamada certa no momento certo.
  - `PartyInvitationLinkEntity.java`: abandona o padrão `@MapsId`/`@OneToOne` "PK=FK, 1 linha por party" (documentado desde o `V3`) em favor de uma PK própria (`id`) + `UNIQUE(party_id, target_role)`, já que agora cabem 2 linhas por party (jogador e espectador). Javadoc atualizado para descrever o novo modelo.
  - `PartyInvitationLinkRepository.java`: `findByTokenHash` inalterado (unicidade global de token); `findForUpdateById(partyId)` → `findForUpdateByPartyIdAndTargetRole(partyId, targetRole)`; novo `findByPartyIdAndTargetRole`.
  - `InvitationService.java`: `getCurrentLink`/`regenerateLink` ganharam o parâmetro `targetRole`; `createInitialLink` passou a criar as duas linhas (`PLAYER` e `SPECTATOR`) na criação da party; `acceptForUser` trocou o papel fixo `PartyRoleType.PLAYER` por `link.getTargetRole()` — agora o papel do novo membro vem do link que ele aceitou, não mais hardcoded. Continua chamando `insertPartyMemberIntoActiveRuns` incondicionalmente; isso já é seguro para um novo espectador graças ao guard adicionado em `insertIntoRun`.
  - `InvitePreviewResponse.java`: ganhou `targetRole`, para o frontend avisar quem está aceitando um convite de espectador antes de criar a conta.
  - `InvitationController.java`: endpoints de jogador **inalterados** (compatibilidade); dois novos, mesmo padrão de sub-rota: `GET/POST .../invitation/spectator` e `.../invitation/spectator/regenerate`.
- **Endpoints adicionados**: `GET /api/parties/{partyId}/invitation/spectator`, `POST /api/parties/{partyId}/invitation/spectator/regenerate`. Nenhum endpoint de jogador/papel foi removido ou teve a assinatura alterada.
- **Backend — teste ajustado**: `InvitationServiceTest.java` — todos os 9 cenários existentes migrados para a assinatura com `targetRole` (default `PLAYER`); novo cenário 10, `acceptingASpectatorLinkAssignsSpectatorRole`, confirmando que aceitar um link com `targetRole=SPECTATOR` atribui `SPECTATOR` em vez do antigo `PLAYER` fixo. Nenhum teste novo foi escrito para `GameChronicleService.create`/`insertIntoRun` especificamente sobre espectadores (fica registrado como pendência, ver abaixo) — os 2 testes existentes de `GameChronicleServiceTest` continuam passando sem alteração, pois cobrem `publish`/`advance`/`completeRun`, não `create`.
- **Frontend alterado**:
  - `frontend/lib/types.ts`: `PartyRole` ganhou `"SPECTATOR"`.
  - `frontend/app/app/party/[partyId]/page.tsx`: `roleLabels` ganhou `SPECTATOR: "Espectador"`; novo botão "Tornar espectador" (`owner` + papel `PLAYER`/`NARRATOR`, com `window.confirm`); o botão "Tornar jogador" já existente passou a também aparecer para `SPECTATOR` (mesmo caminho de volta); filtro "Quem pode editar?" da crônica escrita agora exclui espectadores; novo estado/linha completa de convite de espectador dentro do mesmo card `.invite-members-panel` (copiar/regenerar/WhatsApp, texto temático de "incumbência solene de Observador(a)", buscado em paralelo ao convite de jogador em `load()`).
  - `frontend/app/invite/[token]/page.tsx`: `Preview` ganhou `targetRole`; mostra uma nota extra quando `targetRole === "SPECTATOR"` avisando que a pessoa entrará como Observador(a), só visualizando.
  - `frontend/app/app/party/[partyId]/chronicle/[chronicleId]/page.tsx` (`GameView`): `action(...)` passou a devolver `Promise<boolean>` (sucesso/falha) em vez de `Promise<void>` — todos os chamadores existentes continuam funcionando (TypeScript permite ignorar o retorno em posições que esperam `void`); novo `nextTurn` (turno de sequência `currentSequence + 1`, se existir); novo botão "Publicar, passar e avisar o próximo" no bloco "É a sua vez", que publica e só abre a janela do WhatsApp (`wa.me`, mesmo padrão de `sendInviteWhatsApp`) se a publicação teve sucesso; não aparece quando não há próximo turno (fim da história).
- **Testes executados de fato nesta sessão**:
  - Frontend: `npm run lint` (limpo), `npm test` (62/62 passaram), `npm run build` (compilou, gerou todas as rotas incluindo `/invite/[token]`).
  - Backend: com autorização explícita do utilizador (pedida de novo nesta rodada), toolchain portátil `jdk-21.0.8`+`mvn-3.9.11` — `mvn compile` (sucesso), `mvn test-compile` (sucesso), `mvn test`: **57 testes, 3 falhas**, as mesmas 3 falhas pré-existentes e não relacionadas de sempre em `AiJobStateServiceTest` (confirmado via `git status` que esses arquivos não foram tocados nesta sessão). `InvitationServiceTest` (12/12, incluindo o cenário novo de espectador), `GameChronicleServiceTest` (2/2) e todo o resto passaram.
- **Validações manuais executadas**: nenhuma em navegador real (mesma limitação de todas as sessões anteriores). `/dev-preview` foi regenerado cobrindo os botões "Tornar espectador"/"Tornar jogador" (incluindo o caminho de volta) sobre uma lista de membros fictícia, as duas linhas de convite (jogador + espectador) lado a lado no mesmo card, e o botão "Publicar, passar e avisar o próximo" presente/ausente conforme haja ou não próximo turno.
- **Validações pendentes**:
  - Abrir `/dev-preview` num navegador real (claro e escuro) e confirmar visualmente os 3 itens acima.
  - Testar a migration `V7` contra um Postgres real antes de produção — nunca foi executada nesta sessão, só revisada com cuidado (mesma ressalva já registrada para a `V6` no Console nº8, ainda não confirmada).
  - Escrever testes de backend específicos para a exclusão de espectadores em `GameChronicleService.create`/`insertIntoRun` (comportamento novo sem cobertura de teste dedicada nesta sessão).
  - Confirmar visualmente o texto/tom do convite de espectador ("incumbência solene de Observador(a)") com o utilizador — a redação foi escrita para atender ao pedido de "algo importante", mas pode precisar de ajuste fino.
  - As 3 falhas pré-existentes em `AiJobStateServiceTest` continuam sem investigação (carregadas desde o Console nº7).
- **Estado final**: **Requer validação manual, teste real da migration `V7`, e cobertura de teste adicional no backend**. Lint, testes automatizados de frontend e build de produção passaram de fato. Backend compila e todos os testes tocados por este console passam; a suíte mantém as mesmas 3 falhas pré-existentes e não relacionadas de sempre.

---

### Console nº10 — Editor de texto rico (negrito, itálico, cor, listas) nas histórias

- **Data**: 2026-07-31.
- **Estado do working tree no início**: limpo (Console nº9 já commitado/enviado nesta mesma sessão, a pedido explícito do utilizador antes de iniciar esta feature).
- **Objetivo**: substituir os `<textarea>` de texto puro usados para escrever histórias por um editor de texto rico (negrito, itálico, sublinhado, cor livre, listas com/sem numeração), nos 4 pontos onde conteúdo autoral é digitado: o composer de turno ("Continue a história"), o campo "Início da história" na criação de crônica-jogo, o editor de crônica escrita, e o fluxo de edição de trecho pelo narrador (antes um `window.prompt`, virou um modal de verdade). Pedido do próprio utilizador, que também pediu recomendação de ferramenta e estratégia de backend/banco.
- **Decisões confirmadas com o utilizador antes da implementação**:
  - Formato de armazenamento: HTML sanitizado, nas mesmas colunas `TEXT` já existentes (sem novo tipo de coluna).
  - Conteúdo antigo (texto puro pré-existente): migration única fazendo *escape* de `&`/`<`/`>`, para continuar renderizando igual e em segurança quando a leitura passar a interpretar HTML.
  - Cor: seletor livre (`<input type="color">`), mesmo padrão já usado em `AiTagSettingsPanel.tsx`, em vez de uma paleta fixa.
  - Escopo: os 4 pontos de entrada de conteúdo listados acima (não só o composer de turno).
- **Arquivos lidos antes de planejar**: `docs/PROJECT_AUDIT.md` integralmente, `CLAUDE.md`/`backend/CLAUDE.md`/`frontend/CLAUDE.md`; 1 agente Explore (todos os `<textarea>`, armazenamento/validação de conteúdo no backend, todo ponto de renderização, consumo de conteúdo pelos 3 prompt builders de IA, ausência de qualquer lib de rich-text/sanitizer nos dois lados); 1 agente Plan (desenho detalhado arquivo a arquivo); depois leitura direta de `globals.css` (`.segment-content`/`.story-prose`/`.last-message p`/`input[type="color"]`/`.modal-layer` etc.), `DomainConstants.java`, `backend/pom.xml`, `GameChronicleService.java`, `WrittenChronicleService.java`, `ThreadSegmentRow.tsx`, `StoryModal.tsx`, `AiTagSettingsPanel.tsx`, a página de crônica completa, e os 3 handlers de IA (`CanonMapJobHandler`/`AdaptationJobHandler`/`SynopsisJobHandler`) para confirmar que os 3 montam prompt com o mesmo padrão `.append(segment.getContent())`.
- **Migration criada**: `V8__escape_existing_plain_text_content.sql` — *escape* de execução única (`&`→`&amp;`, `<`→`&lt;`, `>`→`&gt;`, nessa ordem) em `game_segments.content`/`game_drafts.content`/`written_story_documents.content`. `generated_stories`/`chronicle_synopses` ficam de fora (prosa de IA, continua texto puro). **Próximo número seguro agora: `V9`.**
- **Backend — dependências novas**: `com.googlecode.owasp-java-html-sanitizer:owasp-java-html-sanitizer:20240325.1` (allow-list de tags/atributos) e `org.jsoup:jsoup:1.21.1` (usado só para `Parser.unescapeEntities` na extração de texto puro).
- **Backend criado**: `shared/utils/RichTextSanitizer.java` (`@UtilityClass`, sem injeção Spring — não altera assinatura de construtor de nenhum service, então testes existentes que instanciam `GameChronicleService`/`WrittenChronicleService` diretamente não precisaram mudar). `sanitize(html)`: allow-list `b/strong/i/em/u/ul/ol/li/p/br/span`, `span` só com `style` restrito por regex a `color:#rrggbb` (qualquer outra propriedade/valor derruba o atributo inteiro — bloqueia `position:fixed`, múltiplas declarações, etc.); sem `href`/`src`/`class`/`id` em nenhuma tag. `toPlainText(html)`: substitui `</p>`/`<br>`/`</li>` por quebra de linha via regex antes de remover as tags restantes e desescapar entidades (abordagem deliberadamente simples, sem depender do `.text()` do Jsoup, que colapsa espaços em branco e perderia as quebras de parágrafo).
- **Backend alterado**: `GameChronicleService.java` (`create`/`publish`/`saveDraft`/`editSegment` chamam `sanitize()` antes de persistir; `editSegment` sanitiza uma vez e reusa o mesmo valor tanto no segmento vivo quanto no snapshot de `SegmentRevisionEntity`, para os dois baterem exatamente); `WrittenChronicleService.java` (`save` chama `sanitize()`; **bug real encontrado durante o planejamento e corrigido**: `publish()` truncava `document.getContent()` bruto em 600 caracteres para o `generatedPreview` — com HTML isso cortaria tags ao meio e, pior, esse preview é renderizado como texto puro na lista de crônicas, então o utilizador veria `<strong>` literal; trocado para `preview(RichTextSanitizer.toPlainText(...))`); `CanonMapJobHandler.java`/`AdaptationJobHandler.java`/`SynopsisJobHandler.java` (os 3 `buildPrompt()` trocaram `.append(segment.getContent())` por `.append(RichTextSanitizer.toPlainText(segment.getContent()))`, para não poluir o prompt da IA com marcação e não desperdiçar tokens).
- **Frontend — dependências novas**: `@tiptap/react`, `@tiptap/pm`, `@tiptap/starter-kit`, `@tiptap/extension-text-style`, `@tiptap/extension-color` (Tiptap 3.x — resolveu essa major mesmo pedindo `^2`/latest, StarterKit 3.x já inclui `underline`/`link` internamente, então `link` foi explicitamente desligado via `StarterKit.configure({ link: false })` pois o editor não deve permitir links); `isomorphic-dompurify` (**não** o pacote `dompurify` puro — ver bug abaixo).
- **Frontend criado**: `components/RichTextEditor.tsx` (wrapper de `useEditor`, toolbar própria reaproveitando `.invite-icon-button`/`input[type="color"]` já existentes, prop `maxLength` mostra contador de caracteres em texto puro via `editor.getText().length`, expõe `isContentEmpty(html)` — necessário porque um editor "vazio" produz `<p></p>`, não `""`, então os guards `disabled={!draft.trim()}` viraram `disabled={isContentEmpty(draft)}`); `components/RichTextViewer.tsx` (leve, sem instanciar Tiptap — sanitiza com DOMPurify e renderiza via `dangerouslySetInnerHTML`, usado nos pontos de leitura passiva); `components/chronicle/EditSegmentModal.tsx` (substitui o `window.prompt` do narrador, reaproveita as classes `.modal-layer`/`.story-modal`/`.modal-close` já usadas por `StoryModal.tsx`).
- **Frontend alterado**: `chronicle/[chronicleId]/page.tsx` (`GameView`: composer de turno e o bloco "A última mensagem foi" usam `RichTextEditor`/`RichTextViewer`; `edit(segment)` baseado em `window.prompt` foi substituído por estado `editSegmentTarget` + `EditSegmentModal`; `WrittenView`: textarea de conteúdo virou `RichTextEditor` respeitando `readOnly={!lockToken}`); `party/[partyId]/page.tsx` (campo "Início da história" virou `RichTextEditor`, validação `required` nativa trocada por checagem `isContentEmpty` no submit); `ThreadSegmentRow.tsx` (`segment-content` virou `RichTextViewer`, mantendo o texto "Removido pelo Narrador" como está); `globals.css` (`.rte-toolbar`/`.rte-content`/`.rte-char-count` novos; `white-space: pre-wrap` removido de `.segment-content`/`.last-message p` já que `<p>`/`<br>` do HTML agora cuidam da quebra de linha nativamente, com margens novas para `p`/`ul`/`ol`).
- **Bug real encontrado e corrigido durante a verificação (não fazia parte do plano original)**: `RichTextViewer` quebrava a renderização no servidor (SSR do Next.js) com `TypeError: ...dompurify....default.sanitize is not a function` — o pacote `dompurify` puro exige um `window` de navegador real para se auto-configurar, e não existe `window` durante a renderização no servidor. Trocado por `isomorphic-dompurify` (wrapper mantido especificamente para esse cenário, usa `jsdom` internamente no servidor), o que resolve tanto a prévia `/dev-preview` quanto as páginas reais (`GameView`/`ThreadSegmentRow` são renderizadas no servidor antes da hidratação, mesmo sendo `"use client"`). Descoberto rodando o servidor de desenvolvimento de verdade e conferindo `/dev-preview` — não teria aparecido só com `npm run build`/testes automatizados, já que o build estático não exercitou esse caminho de erro em tempo de execução.
- **Backend — testes novos**: `RichTextSanitizerTest` (9 casos: mantém tags permitidas, remove `<script>`/atributos perigosos/`<img>`, aceita `color:#rrggbb` e rejeita qualquer outra propriedade/valor de `style` incluindo declarações múltiplas, `toPlainText` preserva quebras de `<p>`/`<li>`/`<br>` e desescapa entidades, lida com `null`/vazio). `GameChronicleServiceTest` ganhou 2 casos (`publish` remove `<script>` mantendo `<b>`; `editSegment` sanitiza o segmento vivo e o snapshot de revisão de forma idêntica). `WrittenChronicleServiceTest` é um arquivo novo (não existia antes desta sessão) com 2 casos (`save` sanitiza antes de persistir; `publish` gera preview em texto puro sem tags). Os 2 stubs compartilhados em `setUp()` de ambas as classes (`requireMember`/`currentUserService.require()`) tiveram que virar `lenient()` porque nem todo teste da classe os usa (ex.: `editSegment`/`publish` passam por `requireNarrator`, não por `requireMember`) — sem isso o Mockito em modo estrito rejeitava a suíte inteira com `UnnecessaryStubbingException`.
- **Frontend — testes novos**: `RichTextEditor.test.tsx` (`isContentEmpty` isolado + montagem/toolbar/`readOnly`/`onChange` ao digitar), `RichTextViewer.test.tsx` (mantém formatação permitida, remove `<script>`/`onerror`/`<a href="javascript:...">`, aplica `className`), `EditSegmentModal.test.tsx` (fechado não renderiza nada, pré-preenche do `segment.content`, cancela sem chamar `onSave`, salva com o motivo editado). `ThreadSegmentRow.test.tsx` ganhou 2 casos (conteúdo formatado com `<strong>`; payload com `<script>`/`<img onerror>` não sobrevive no DOM). `vitest.setup.ts` ganhou polyfills novos (`document.elementFromPoint`, `Range.prototype.getBoundingClientRect`/`getClientRects`) porque o jsdom não tem motor de layout e o ProseMirror (núcleo do Tiptap) precisa medir posições reais mesmo em teste — sem isso, digitar num editor montado em teste lançava exceções não tratadas.
- **Testes executados de fato nesta sessão**:
  - Frontend: `npm run lint` (limpo), `npm test` (80/80 passaram, incluindo os 4 arquivos novos), `npm run build` (compilou, TypeScript passou, todas as rotas geradas incluindo `/dev-preview`).
  - Backend: com autorização explícita do utilizador, toolchain portátil `jdk-21.0.8`+`mvn-3.9.11` — `mvn compile` (sucesso), `mvn test-compile` (sucesso), `mvn test`: **70 testes, 3 falhas**, as mesmas 3 falhas pré-existentes e não relacionadas de sempre em `AiJobStateServiceTest` (confirmado via `git status --porcelain` que esse arquivo não foi tocado nesta sessão). `RichTextSanitizerTest` (9/9), `GameChronicleServiceTest` (4/4), `WrittenChronicleServiceTest` (2/2, arquivo novo) e todo o resto passaram.
- **Validações manuais executadas**: servidor de desenvolvimento (`npm run dev`) foi de fato iniciado e `/dev-preview` foi consultado via HTTP real nesta sessão (não só revisado no código) — foi assim que o bug de SSR do DOMPurify acima foi encontrado e depois confirmado corrigido (HTTP 200, seções novas presentes no HTML). `/dev-preview` foi regenerado cobrindo os 3 novos componentes: `RichTextEditor` (editável e `readOnly`), `RichTextViewer` (conteúdo formatado de exemplo) e `EditSegmentModal` (botão que abre o modal pré-preenchido). Nenhuma inspeção visual manual num navegador de verdade (clique em botões da toolbar, seleção de cor, etc.) foi feita — só a checagem HTTP de que a página renderiza sem erro.
- **Validações pendentes**:
  - Abrir `/dev-preview` num navegador real (claro e escuro) e testar interativamente a toolbar (negrito/itálico/sublinhado/cor/listas), o modal de edição de trecho, e o comportamento `readOnly` da crônica escrita.
  - Testar a migration `V8` contra um Postgres real antes de produção — nunca foi executada nesta sessão, só revisada com cuidado (mesma ressalva já registrada para `V6`/`V7`, ainda não confirmadas).
  - Nenhum teste novo foi escrito para os 3 `buildPrompt()` dos handlers de IA (`CanonMapJobHandler`/`AdaptationJobHandler`/`SynopsisJobHandler`) confirmando que `toPlainText()` está de fato sendo usado — a mudança foi verificada por leitura direta do código, não por teste automatizado, porque nenhum dos 3 handlers tem um arquivo de teste dedicado hoje (fica registrado como pendência).
  - Nenhuma chamada real à OpenAI foi feita para confirmar que os prompts com texto puro (em vez de HTML) continuam produzindo resultados de qualidade equivalente.
  - As 3 falhas pré-existentes em `AiJobStateServiceTest` continuam sem investigação (carregadas desde o Console nº7).
- **Estado final**: **Requer validação manual interativa e teste real da migration `V8`**. Lint, testes automatizados de frontend (incluindo um bug de SSR real encontrado e corrigido rodando o servidor de verdade) e build de produção passaram de fato. Backend compila e todos os testes tocados por este console passam; a suíte mantém as mesmas 3 falhas pré-existentes e não relacionadas de sempre.

---

## 13. Próximo console recomendado

Recomenda-se que o próximo console execute o item **9 da lista original (revisão integrada das funcionalidades)** antes de iniciar qualquer feature nova. Justificativa: o núcleo de jogo (turnos, ciclos, segmentos, IA) tem quase nenhuma cobertura de teste automatizado e é a parte mais rica em invariantes de negócio do projeto; validar esse núcleo ponta a ponta (manualmente e/ou com testes novos) antes de empilhar votação, PWA e notificações reduz o risco de construir sobre uma base não verificada. Isso não segue estritamente a ordem numérica original da lista (que colocaria "página da história finalizada" em seguida), mas a dependência técnica real justifica essa priorização — ver a sequência recomendada completa em §6.

Alternativa aceitável, se o utilizador preferir progresso de produto visível antes de revisão técnica: iniciar diretamente pelo item 2 (página da história finalizada), que não tem dependências bloqueantes e reaproveita componentes já existentes.

**Atualização (Console nº2, 2026-07-30)**: o item 2 (página da história finalizada) foi implementado neste console, ainda pendente de validação visual manual (ver acima). Confirmado que a sequência técnica do CLAUDE.md deste console ("próximo console: Extração automática de tags") é compatível com o roadmap já registrado em §6 — o item 3 (extração automática de tags) é agora o próximo passo natural, pois destrava o `CanonMapPanel` (hoje um placeholder honesto de estado vazio). Recomendação: **item 3 (extração automática de tags)** como próximo console, com a alternativa de primeiro executar o item 9 (revisão integrada) se o utilizador preferir consolidar cobertura de testes do núcleo de jogo antes de somar mais uma feature de IA.

**Atualização (Console nº3, 2026-07-30)**: o item 2 foi refinado (não reaberto do zero) por pedido direto do utilizador — Mapa do cânone colapsável/reposicionado, história adaptada em modal com histórico de versões, e uma nova barra de progresso de turnos. O `CanonMapPanel` continua exatamente o mesmo placeholder honesto de antes (decisão explícita do utilizador neste console, não uma pendência técnica) — a recomendação do Console nº2 permanece válida e inalterada: **item 3 (extração automática de tags)** é o próximo passo natural para destravá-lo, com a mesma alternativa de primeiro executar o item 9 (revisão integrada) se o utilizador preferir consolidar testes do núcleo de jogo antes disso. Pendência nova a carregar para o próximo console: nenhum teste de backend foi adicionado para a nova query agregada de progresso (`GameTurnRepository.findProgressByChronicleIds`) nem para o novo endpoint de histórico de versões — considerar cobrir isso caso o item 9 seja executado antes do item 3.

**Atualização (Console nº4, 2026-07-30)**: console de ajustes finos (polish + 1 bug real corrigido + logging), não alterou o roadmap do produto — a recomendação de **item 3 (extração automática de tags)** para o próximo console de feature nova permanece válida e inalterada. Pendências de teste de backend acumuladas para quando o item 9 (revisão integrada) for executado: além das duas já listadas na atualização do Console nº3, agora também `ChronicleService.list()` (ordenação/`awaitingCurrentUser`) e `GameTurnRepository.findActiveTurnUsersByChronicleIds` ficaram sem cobertura de backend.

**Atualização (Console nº5, 2026-07-30)**: console de correções de UX só no `StoryModal` (sem backend, sem migration), não altera o roadmap. Nova pendência não-bloqueante registrada em §12: ausência de polling automático enquanto um job de IA está `AI_PENDING`/`AI_PROCESSING` — o utilizador só vê o estado atualizar ao recarregar/agir; considerar resolver (polling leve ou WebSocket) se isso se mostrar confuso na prática, não necessariamente no próximo console.

**Atualização (Console nº6, 2026-07-30)**: implementado o pipeline de IA após a conclusão (mapa do cânone + adaptação + sinopse), substituindo "Extração automática de tags" e absorvendo "Mini-história manual" (ver §6). O item 3 do roadmap original está portanto **concluído, pendente de validação manual/execução de testes** (ver §12). A ausência de polling automático (registrada no Console nº5) agora também se aplica ao mapa do cânone e à sinopse — o `CanonMapPanel` só atualiza ao recarregar a página, mesma limitação pré-existente, não resolvida neste console. Recomendação para o próximo console: **executar `mvn test` num ambiente com acesso ao mirror Maven correto** (ou IntelliJ) para confirmar de fato a nova suíte de testes de backend antes de somar mais funcionalidade sobre este pipeline; em seguida, **item 5 e/ou 7 do roadmap (votos em histórias / votos em tags)**, que agora têm uma base real de tags para votar (antes dependiam de um placeholder). Item 9 (revisão integrada) continua uma alternativa aceitável se o utilizador preferir consolidar testes do núcleo de jogo antes de mais features.

---

**Atualização (Console nº7, 2026-07-31)**: console de ajustes de UX/UI pedidos diretamente pelo utilizador (participação por crônica, timeload de IA, divergência entre "Re-executar IA"/"Regenerar história", polish de menu/botões, página de backend fora do ar) — não fazia parte do roadmap de 19 itens e não o altera; a recomendação de consolidar testes (item 9) ou seguir para votos (itens 5/7) permanece válida para quando o utilizador quiser retomar o roadmap original. Achado novo e relevante: `mvn test` finalmente conseguiu rodar neste ambiente (antes bloqueado pelo mirror Maven privado, ver §11) e revelou 3 falhas pré-existentes em `AiJobStateServiceTest`, não relacionadas a este console (confirmado via `git status` — esses arquivos não foram tocados). Recomendação para o próximo console: investigar essas 3 falhas antes de somar mais funcionalidade sobre o pipeline de IA, e considerar escrever testes de backend para `leaveChronicle`/`rejoinChronicle`/`reRunAi` (adicionados neste console sem cobertura de teste de backend).

**Atualização (Console nº8, 2026-07-31)**: continuação do Console nº7 na mesma sessão — 4 ajustes de polish (frontend) mais a substituição das categorias fixas do Mapa do cânone por categorias dinâmicas por party (backend + frontend, migration `V6`). Não faz parte do roadmap de 19 itens original e não o altera. **Atenção para o próximo console**: a migration `V6` nunca foi executada contra um banco Postgres real nesta sessão (só revisada com cuidado) — antes de qualquer deploy, rodá-la primeiro num ambiente de teste, já que ela contém `DROP TABLE party_ai_tag_settings` e `DROP COLUMN` irreversíveis em `canon_map_generation_categories`/`canon_tags`. As 3 falhas pré-existentes em `AiJobStateServiceTest` continuam sem investigação (carregadas desde o Console nº7).

**Atualização (Console nº9, 2026-07-31)**: papel de Espectador (view-only, nunca participa de ciclos), convite de espectador paralelo ao de jogador (migration `V7`), e botão de aviso do próximo turno por WhatsApp. Não faz parte do roadmap de 19 itens original e não o altera. **Atenção para o próximo console**: nem a `V6` (Console nº8) nem a `V7` (este console) foram executadas contra um Postgres real ainda — testar as duas antes de qualquer deploy; `GameChronicleService.create`/`insertIntoRun` ganharam lógica de exclusão de espectadores sem teste de backend dedicado; as 3 falhas pré-existentes em `AiJobStateServiceTest` continuam sem investigação.

**Atualização (Console nº10, 2026-07-31)**: substituição dos `<textarea>` de conteúdo autoral por um editor de texto rico (Tiptap: negrito/itálico/sublinhado/cor livre/listas), com sanitização HTML server-side (`RichTextSanitizer`, novo) e migration `V8` de *escape* do conteúdo antigo. Não faz parte do roadmap de 19 itens original e não o altera. Esta é a primeira feature da aplicação com uma superfície real de XSS — sanitização server-side é a autoridade, DOMPurify no cliente é defesa adicional. **Atenção para o próximo console**: `V6`/`V7`/`V8` seguem todas sem execução contra um Postgres real (testar as três antes de qualquer deploy); os 3 `buildPrompt()` de IA foram atualizados para usar texto puro em vez de HTML mas nenhum deles tem teste de backend dedicado (mudança verificada só por leitura de código); validação manual interativa da toolbar/modal num navegador real ainda não foi feita (só a checagem HTTP de que a página renderiza); as 3 falhas pré-existentes em `AiJobStateServiceTest` continuam sem investigação (carregadas desde o Console nº7). Achado de processo relevante: rodar o servidor de desenvolvimento de verdade (não só `npm run build`/testes automatizados) revelou um bug real de SSR (`dompurify` puro quebra sem `window` do navegador; corrigido trocando para `isomorphic-dompurify`) que build+testes não teriam pego — vale considerar isso como prática padrão ao introduzir uma lib que só roda no cliente.

Todo console futuro deve começar lendo: (1) todos os `CLAUDE.md` aplicáveis; (2) `docs/PROJECT_AUDIT.md` (este arquivo) integralmente; (3) o estado atual do Git.
